# Mushitori ブラッシュアップ差分メモ（v0.1.0 → v0.2.0）

このzipは、アップロードいただいたソース一式（v0.1.0）に対して、以下3点のブラッシュアップを
加えたものです。差分がある/新規追加したファイルは以下の通りです。

## 変更・追加ファイル一覧

- 変更: `src/main/java/jp/mushitori/MushitoriPlugin.java`
- 変更: `src/main/java/jp/mushitori/listener/FishingListener.java`
- 変更: `src/main/java/jp/mushitori/listener/NetListener.java`（全面書き換え）
- 変更: `src/main/java/jp/mushitori/listener/PlayerDataListener.java`
- 変更: `src/main/java/jp/mushitori/registry/GearRegistry.java`
- 変更: `src/main/java/jp/mushitori/service/DexService.java`
- 新規: `src/main/java/jp/mushitori/service/AdvancementService.java`
- 新規: `src/main/java/jp/mushitori/service/ApproachFishingService.java`
- 変更: `src/main/resources/config.yml`
- 変更: `src/main/resources/creatures.yml`
- 変更: `src/main/resources/plugin.yml`（バージョンを0.2.0に）
- 新規: `src/main/resources/mushitori_datapack/**`（進捗用の同梱データパック一式）

他のファイル（`Keys.java` / `CatchData.java` / `Category.java` / `Creature.java` / `Rarity.java` /
`CreatureRegistry.java` / `CatchService.java` / `MoneyService.java` / `SpawnService.java` /
`DexManager.java` / `PlayerDex.java` / `GuideListener.java` / `DexGui.java` / `GuiListener.java` /
`MushitoriCommand.java` / `pom.xml`）は変更していません。

---

## 1. 実績 ⇔ Minecraft進捗（アドバンスメント）連携

- `AdvancementService` を新設。プラグインに同梱した進捗データパック
  （`mushitori_datapack/data/mushitori/advancement/*.json`）を、起動時に
  `world/datapacks/mushitori_advancements/` へ自動コピーし、コンソールから
  `datapack enable "file/mushitori_advancements"` を実行して読み込ませます。
- 進捗ツリー: `root`（図鑑タブ）→ `first_catch` → `complete_bug` / `complete_fish_rod` /
  `complete_fish_approach`（それぞれ goal）→ `complete_all`（challenge）、
  `first_catch` から `ur_hunter`（challenge）・`catch_100`（goal）も分岐。
  既存の `DexService.Achievement` の7種（first_catch / complete_bug /
  complete_fish_rod / complete_fish_approach / complete_all / ur_hunter / catch_100）と
  IDを完全一致させています。
- 各JSONの criteria は `minecraft:impossible` トリガー固定です。つまり通常プレイでは
  絶対に達成されず、実績達成時に `DexService#checkAchievements` から
  `AdvancementService#grant(player, id)` を呼んで初めて進捗が付与されます。
- ログイン時（`PlayerDataListener`）に `DexService#syncAdvancements` を呼び、
  データパック未読み込みのタイミングで実績だけ達成していた、というズレを解消します。
- GUI側（`DexGui`）はもともと `達成: yyyy/MM/dd HH:mm` の形で達成日時を表示済みだったため
  変更していません。

### 動作確認のしかた
1. サーバーを起動し、ログを確認（「進捗データパックを書き出しました」というログが出ます）。
2. `/datapack list` で `file/mushitori_advancements` が enabled になっているか確認。
   自動有効化が効かない場合は手動で `/datapack enable "file/mushitori_advancements"` を実行してください。
3. `/advancement grant <プレイヤー> only mushitori:root` などで手動付与できるか確認、
   もしくは実際に何か1匹図鑑登録して `first_catch` が進捗タブに出るか確認。

### 既知の注意点
- `pack.mcmeta` の `pack_format` は執筆時点で判明している値を仮置きしています
  （`supported_formats` で広めの範囲を許容してはいますが）。Paper 26.2の実際の値と
  ズレていて読み込みエラーになる場合は、サーバーログを見て調整してください。
- advancementのJSONスキーマ（`icon` の書式など）もバージョンで変わることがあります。
  読み込みエラーが出た場合は、該当ファイルをエラーメッセージに合わせて調整してください。
- `/datapack enable` の自動実行はサーバー起動20tick後に実行しています。ワールドの
  読み込みが極端に遅い環境では、起動ログで有効化に失敗していないか確認してください。

---

## 2. 虫取り網（右クリック化・耐久値・演出）

- `NetListener` を全面書き換え。`EntityDamageByEntityEvent`（攻撃）ではなく
  `PlayerInteractEvent` の右クリックで発動するようにしました。
- ベース素材を `STICK` → `CARROT_ON_A_STICK` に変更（config.ymlの`nets`セクション）。
- 各tierに `durability`（耐久値）を追加。0以下で無限耐久。
  `ItemMeta` の `Damageable#setMaxDamage` / `setDamage` を使ったバニラ標準の耐久システムです。
- 右クリックすると、プレイヤーの視線方向・正面の狭い範囲（距離・角度は
  `config.yml` の `net-catching` セクションで調整可）にいる複数のいきものを
  まとめて捕獲判定します（`max-targets` で同時捕獲数の上限も設定可）。
- 演出: `Particle.SWEEP_ATTACK` ＋ `Sound.ENTITY_PLAYER_ATTACK_SWEEP`（バシュッ、という
  スイープ攻撃と同じ音）を毎回鳴らします。何も捕まらなかったときの文言表示はありません。
- 何か1匹でも捕まったときだけ `Sound.ENTITY_EXPERIENCE_ORB_PICKUP`（チリン）を追加で鳴らします。
- 耐久値は右クリック（スイング）ごとに1消費。0になった時点でアイテムを消し、
  破壊音（`ITEM_BREAK`、config変更可）を鳴らします。
- 連打対策として、config.ymlの`cooldown-ms`（既定250ms）でクールダウンを設けています。
- いきものエンティティは、殴っても矢を当てても倒せないよう`EntityDamageEvent`を
  常時キャンセルする形は維持しています（捕獲トリガーだけを右クリックに変更）。

### 設計上の割り切り（要相談ポイント）
- 「正面の狭い範囲」は視線方向とのなす角度＋距離のみで判定しており、壁越しの
  いきものも巻き込める可能性があります（レイキャストによる遮蔽チェックは未実装）。
  必要であれば追加できます。
- 耐久値はスイングのたびに1消費する設計にしています（捕まえられたかどうかは問わない）。
  もし「捕まえたときだけ耐久値を減らす」方が望ましければ、`swing()`内の
  `consumeDurability` 呼び出し位置を `if (caught > 0)` ブロックの中に移すだけで変更できます。

---

## 3. 魚の処理（試験実装: FISH_APPROACH）※ v0.2.1で仕様変更

**v0.2.1時点の仕様**（ユーザー要望を反映して再設計）:
- 釣竿をキャスト（`PlayerFishEvent.State.FISHING`）した瞬間に抽選し
  （`fishing.approach.trigger-chance`、既定35%）、当たりなら浮きの近く
  （`search-radius`、既定8ブロック）に**すでに実際に泳いでいる**バニラの魚エンティティを
  1匹探します（デコイを新規スポーンする旧方式から変更）。対象は`creatures.yml`の
  FISH_APPROACHに登録した生物の`entity`フィールドと一致する種類のみです
  （例: TROPICAL_FISH / SALMON / COD）。近くにいなければ何も起きません。
- 見つけた魚のバニラAIを一時的に止め（`setAI(false)`）、プラグイン側の制御で
  少しずつ浮きへ座標を近づけていきます（`min-seconds`〜`max-seconds`の範囲で
  ランダムな到達時間）。
- 浮きのそば（`arrival-distance`、既定1.2ブロック）まで来たら「振るタイミング」の
  合図（`sound-window-start`、既定は魚が食いついた時のバニラ音）を鳴らし、
  `window-seconds`（既定1.2秒）だけ受け付けます。
- この間にプレイヤーが竿を振る（リールを巻く）と捕獲成功。タイミングが早すぎた場合や
  猶予を過ぎた場合は、魚は（消えずに）逃げていきます。「失敗した」という表示は出しません。
- 「竿を振った」の検知は`PlayerFishEvent`の状態変化（`CAUGHT_FISH` / `CAUGHT_ENTITY` /
  `FAILED_ATTEMPT` / `REEL_IN`）を`FishingListener`が監視し、`ApproachFishingService#onReelAttempt`
  を呼ぶ形で行っています。ちょうどタイミングが合っていた場合、通常の釣り（FISH_ROD）側の
  抽選・釣果は打ち消し、二重取得を防いでいます。
- 複数プレイヤーが近くで同時に釣りをしても同じ魚を取り合わないよう、誘導中の魚のUUIDを
  予約リストで管理しています。

### 動作確認のしかた
- 海・川など、対象のバニラ魚（`creatures.yml`で設定した`entity`）が自然湧きしている場所、
  もしくは `/summon tropical_fish` 等で試験的に湧かせた場所で釣りをしてください。
- キャスト後、近くにその種類の魚がいれば誘導が始まります（見た目上、魚が少しずつ浮きに
  寄ってくる動きになります）。浮きのそばまで来たら音が鳴るので、そのタイミングで
  もう一度右クリックしてリールを巻いてください。

### 設計上、まだ決め切っていない点（ご相談したいポイント）
- 猶予時間内であれば、竿を振った瞬間の正確さ（早い/遅い）による判定はしておらず、
  ウィンドウ内なら常に成功する単純な二択（当たり/外れ）にしています。もっと
  シビアなタイミング判定（例えば「合図から0.2〜0.5秒後がジャストタイミング」）に
  したい場合は調整可能です。
- 誘導中の魚が障害物（地形・ブロック）を無視してまっすぐ浮きに向かう単純な直線移動です。
  障害物回避や、より自然な遊泳ルートにしたい場合は追加実装が必要です。
- 猶予時間を過ぎて魚が逃げたあと、その魚はまだ近くにいるので再度同じキャストで
  誘導対象に選ばれる可能性があります（連続で失敗し続けると同じ魚を何度も狙うことになる）。
  気になる場合は「一定時間は同じ魚を対象から除外する」仕組みを追加できます。

---

### v0.2.0時点の旧仕様（参考・廃止）
最初のブラッシュアップ版では、見た目用のダミー生物をその場に新規スポーンさせて
自動で捕獲する方式でした。「実在する魚を誘導する」「タイミングよく竿を振って取る」という
今回のご要望を受けて、上記の新仕様に置き換えています。

---

## 4. v0.2.2での追加修正（2ラウンド目のフィードバック対応）

### 魚が寄ってこない問題
旧仕様は「浮きの近くにすでに実在する本物の魚」しか探しておらず、その場に該当する
バニラ魚が自然湧きしていなければ何も起きませんでした。虫と同じ`SpawnService`
（`/mushitori spawn`と同じ、creature_idタグ付きスポーン）を流用し、近くに見つからなければ
FISH_APPROACHから1種ランダムに選んでその場に湧かせるフォールバックを追加しました
（`config.yml`の`fishing.approach.spawn-if-missing`で無効化も可能）。
湧かせる場所は、浮き周辺で水ブロックを軽く探索し、見つかればそこを優先します。

これにより、`creatures.yml`のFISH_APPROACHに設定した`entity`（COD / SALMON /
TROPICAL_FISHなど）は、虫と同様に「種類を設定しておけばspawnされる」対象になりました。

### 虫取り網が空中で効きにくい問題
`NetListener`の索敵範囲が、プレイヤーの足元を中心にした立方体（`Player#getNearbyEntities`）を
使っていたため、目線（足元より約1.6ブロック上）より上方向の実効範囲が狭くなっていました。
目線位置を中心にしたバウンディングボックス（`World#getNearbyEntities(BoundingBox)`）で
探すように変更し、上や下を向いたときも同じ距離だけ捕獲できるようにしています。

---

## 5. v0.2.3での追加修正: 虫取り網が「空中を向くと発動しない」問題

前回の索敵範囲の修正（目線中心のバウンディングボックス）は行ったものの、実際には
それ以前の問題として、**何もない方向（ブロックが無い空中）を向いて右クリックしても
`PlayerInteractEvent`自体が発火していなかった**ことが分かりました。

原因は、虫取り網のベース素材に `CARROT_ON_A_STICK`（にんじん付き棒）を使っていたことです。
このアイテムはブタ・ストライダーに乗って操縦するための特殊な「使用」挙動を持っており、
対象（ブロックなど）が無い状態での右クリックだと、サーバー側でイベントが発火しない
ケースがあります。

対応として、ベース素材を `STICK`（v0.1.0と同じ、特殊挙動のない素材）に戻しました。
見た目は `item.model`（リソースパック側のカスタムモデル）で決まる部分なので、
ベース素材をSTICKに戻しても、リソースパックさえ当たっていれば「にんじん付き棒」の
見た目自体は維持できます。config.ymlで`material`を変更することは引き続き可能ですが、
`CARROT_ON_A_STICK`や`FISHING_ROD`など右クリックに特殊な挙動を持つ素材は、この空中
クリック問題が起きる可能性があるため非推奨としてコメントを追記しています。

---

## 6. v0.2.4での追加修正: コンパイルエラー（実質的にfinalでない変数のラムダ参照）

`ApproachFishingService#maybeStart`内で、`fish`変数を条件分岐の中で再代入していたため
（近くに実在する魚が見つからなかった場合に`fish = plugin.spawnService().spawn(...)`で
代入し直していた）、その後にラムダ式（`runAtFixedRate`の中身）から`fish`を参照している
箇所が「実質的にfinal」の制約に違反しコンパイルエラーになっていました。

再代入が終わって値が確定した後に`final Entity lureFish = fish;`として確定値をコピーし、
ラムダ内はすべて`lureFish`を参照するように修正しました。

---

## 7. v0.2.5での追加修正: 空中右クリックが発動しない／魚が動いて見えない

### 虫取り網が空中で発動しない（素材変更でも直らなかった）
素材（STICK / CARROT_ON_A_STICK）の問題ではなく、**空中への右クリックも毎回
`event.setCancelled(true)`していたこと**が原因だった可能性が高いです。ブロックに対する
右クリックをキャンセルするのは正常な操作ですが、空中への右クリックにはキャンセルする
理由（防ぐべきバニラ動作）がそもそもなく、それでもキャンセルし続けると、Bukkit/Paper側で
「それ以降の空中への右クリック」自体が発火しなくなることがある、という既知のクセに
該当する可能性があります。

`Action.RIGHT_CLICK_BLOCK` のときだけ `setCancelled(true)` するよう修正し、空中への
右クリックはキャンセルしないようにしました。

### 魚が寄ってくる動きが見えない
`spawn-if-missing`によって魚を新しく湧かせる際、近くに水ブロックが見つからないと
「浮きの位置そのもの」にフォールバックして湧かせていました。この場合、湧いた瞬間に
すでに到達済み（`arrival-distance`以内）になってしまい、動く前に静止してしまいます。

水ブロックが見つからなくても、必ず浮きから一定距離（探索半径の半分以上）離れた場所に
湧かせるよう修正し、寄ってくる動きが見えるようにしました。

### 動作確認用のデバッグログを追加
`config.yml` の `fishing.approach.debug` を `true` にすると、抽選結果・誘導対象・
到達／タイムアウトのタイミングをコンソールログ（`[fishing.approach] ...`）に出すように
しました。今回の修正後もまだ動かない場合は、これを `true` にして再現させ、ログを
共有してもらえると原因の切り分けがしやすくなります。

---

## 8. v0.3.0: 魚の処理（FISH_APPROACH）を「待って狙う」仕様に再設計

これまでの試験実装は「キャストのたびに抽選し、当たれば魚を誘導する（見つからなければ
自動で湧かせる）」という単発の仕組みでした。今回、以下の設計意図に沿って作り直しました。

- **通常の釣り（FISH_ROD）＝易しくてベター**：いろいろな種類の、低レア度の魚が出やすい
  （既存のまま、特に変更なし）
- **モブ直接釣り（FISH_APPROACH）＝難しいが良いものが釣れる**：じっくり待つ必要があり、
  数も少ない・限定の魚として扱う

### 主な変更点

1. **浮きを投げた瞬間の自動スポーンを廃止**しました。対象になるのは、あらかじめ
   `/mushitori spawn <id>`（または今後作る専用コマンド）でワールドに置いた、
   `creature_id`タグ付きのFISH_APPROACH個体だけです。タグなしの自然湧き魚は対象外です。
2. **「待つ」を段階式にしました**：
   - キャスト時、浮きの近く（`notice-radius`）に対象個体がいるか確認（いなければ何も起きない）
   - `patience-seconds`（既定15秒）じっと待つと、`trigger-chance`（既定25%）の判定が始まる
   - 外れたら`retry-interval-seconds`（既定5秒）おきに再判定し続ける（待てば待つほどチャンスがある）
   - 判定に成功すると、そこから初めて「浮きへ近づいてくる」誘導が始まる（これ以降の
     流れ＝到達→振るタイミングの合図→猶予時間内に竿を振る、という部分は従来どおり）
3. **通常釣りより有利になる専用ボーナス**を追加しました。`extra-size-bonus` /
   `extra-rare-bonus`が、竿自体のボーナスに上乗せされます（0にすれば通常釣りと同条件）。
   レア度抽選テーブル自体は共通のままなので、これがないと「良いものが出やすい」が
   実現できないため追加しています。
4. 「数が少ない・限定」は、捕まえると個体そのものが消える（管理者が置き直すまで
   存在しない）という既存の仕組みでそのまま表現されます。追加のコードは不要でした。
5. `creatures.yml`のFISH_APPROACHサンプル（ニシキゴイ・イワナ）のdescription・habitatを、
   新しい仕様（`/mushitori spawn`前提）に合わせて書き直しました。

### 今後ご相談したい点
- 現状、`/mushitori spawn`は管理者コマンドです。プレイヤー自身が「餌をまいて呼び込む」
  ような能動的な手段は用意していません。運用上、管理者が定期的に補充する想定で
  問題ないか、それとも自動湧き（例えばワールドの特定エリアで一定時間ごとに1匹だけ
  湧くなど）を別途用意したほうがよいか、方向性を教えてください。
- `patience-seconds`と`retry-interval-seconds`は全FISH_APPROACH共通の設定です。
  生物ごとに難易度を変えたい（レア個体ほど待ち時間を長くする、など）場合は、
  `creatures.yml`側に個別の上書き項目を追加できます。

---

## 9. v0.4.0: レア度・サイズの仕組みを全面再設計

これまで「捕まえた瞬間にすべて抽選する」方式でしたが、以下の方針で作り直しました。

### 2段階抽選に変更
- **素の特性**：`/mushitori spawn`などでいきものが湧いた瞬間に、そのいきもの1匹固有の
  「レア度」「サイズ段階」を1回だけ抽選し、エンティティのPDCに焼き付けます
  （バニラ釣り＝FISH_ROD は、捕まえる瞬間まで個体が存在しないため、これまでどおり
  捕まえる瞬間にまとめて抽選します）。
- **道具ボーナス**：実際に捕まえる瞬間、網・竿のボーナスぶん確率で「レア度」「サイズ段階」を
  それぞれ1段階だけ引き上げます（サイズは常に「より大きい」方向へ）。

### レア度をPDCタグ（summering名前空間）で処理
`Keys.java`のPersistentDataContainerの名前空間を、プラグイン名（mushitori）ではなく
固定文字列`summering`に変更しました。**これは破壊的変更です**。これまでにワールド上に
出ているアイテムやモブ（`mushitori:xxx`タグ付き）は、更新後は図鑑・売却・網での捕獲などで
一切認識されなくなります。テスト用に置いていたアイテム・モブは、アップデート後に
作り直してください。

※ 進捗（アドバンスメント）データパック側の名前空間（`mushitori:root`等）は、今回は
`mushitori`のまま変更していません。データパックも`summering`に揃えたい場合は教えてください。

### サイズを7段階のランクに変更
`config.yml`に新しい`sizes`セクションを追加しました。小さい順に
`最もちいさい / とてもちいさい / ちいさい / ふつう / おおきい / とてもおおきい / 最もおおきい`
の7段階（キー名は自由に変更可）。各段階の`weight`（出現しやすさ）と`price-mult`（価格倍率）を
指定できます。例として、ふつうが最も出やすく、両端（最も〜）がほとんど出ない重み付けを
デフォルトで入れています。

サイズ段階が決まったら、そのいきものの`size-min`〜`size-max`をサイズ段階の数（7）で
均等に区切った範囲内から、実際のcm値をランダムに1つ選びます。図鑑・アイテムのロアには、
サイズ段階の名前と実測cmの両方を表示するようにしました。

売値は「基本価格 × レア度のprice-mult × サイズ段階のprice-mult」に変更しています
（従来の「cmに応じた連続倍率」は廃止しました）。

### レア度抽選テーブルを生物ごとに持てるように
`creatures.yml`の各生物に、任意で`rarities`セクションを書けるようにしました。書式は
`config.yml`の共通`rarities`と同じです。省略した生物は共通テーブルをそのまま使います。
例として、カブトムシに「めずらしい」が出やすい専用テーブルを設定しています
（カナブンは共通テーブルのまま＝レアも出ることがあります）。

**注意**：専用テーブルを書く場合も、キー名（例: N/R/SR/UR）は共通テーブルと揃えてください。
実績「ウルトラレアハンター」などは`"UR"`というキー名を直接見ているため、キー名を変えると
判定できなくなります。

### FISH_APPROACHの難易度を個体ごとに上書き可能に
`creatures.yml`のFISH_APPROACH個体に、任意で`approach`セクションを書けるようにしました。
`patience-seconds`（待ち時間）・`retry-interval-seconds`（再判定間隔）・`trigger-chance`
（反応確率）・`min-approach-seconds`/`max-approach-seconds`（寄ってくる速さ）・
`window-seconds`（振れる猶予）を個別に指定できます。例としてニシキゴイに、共通設定より
少し厳しめの設定を入れています。

### 逃げられたときの後始末（煙で消滅 or 水辺へ再配置）
`config.yml`の`fishing.approach.escape`セクションで選べます。
- `RELOCATE`（既定）：AIを元に戻し、近くの水辺を探してそこへ移動させる。個体は消えず、
  焼き付けたレア度・サイズはそのまま保持されるので、また誰かが挑戦できます。
- `SMOKE_DESPAWN`：煙のパーティクルとともにその場で消滅させる。管理者が
  `/mushitori spawn`で置き直すまでいなくなります。

### 図鑑（GUI）の表示
実測サイズ（最大/最小cm）に加えて、「見つけたサイズ段階」の一覧（ちいさい／おおきい 等、
実際に出たものだけを表示）を追加しました。ご要望どおり、cmとサイズ段階の両方を記載しています。

### 補足（こちらで判断した点）
- 道具の「大物ボーナス」は、サイズについては常に「より大きい」方向へ1段階引き上げる形に
  しています（小さい方向の極端は、あくまで素の抽選の重みだけで出ます）。逆方向へのボーナスも
  欲しい場合はご相談ください。
- レア度・サイズ段階とも、上位への引き上げは常に「configの並び順で1つ次」です。今回の
  重み付け・並び順であれば違和感なく機能するはずですが、順序を変える場合はご注意ください。

---

## 10. v0.4.1: フィードバック反映（双方向ボーナス／summering統一／魚の演出改善）

### 大物ボーナスを双方向に
`nets`/`rods`の`size-bonus`を符号付きの値として扱うようにしました。正の値で「より大きい」
方向、負の値で「より小さい」方向へ、確率で1段階寄せます（絶対値がその確率）。
`SizeTier.Table`に`downgrade()`/`shift()`を追加し、`CatchService.applyBonus`はこちらを
使うようにしています。既存のtier1〜3の値はすべて0または正のままなので、動作は変わりません。
「小さいものが出やすい網／竿」を作りたい場合は、`size-bonus`に負の値を設定してください。

### 進捗データパックもsummering名前空間に統一
`AdvancementService`と、同梱データパックの中身（`data/mushitori/advancement/` →
`data/summering/advancement/`、フォルダ名も`mushitori_datapack` → `summering_datapack`）を
`summering`名前空間に揃えました。`config.yml`の`advancements.pack-name`の既定値も
`summering_advancements`に変更しています。

**これも破壊的変更です**。以前のバージョンで進捗データパックをすでに読み込ませていた場合、
古い`world/datapacks/mushitori_advancements/`フォルダは手動で削除してください（自動削除は
していません）。新しいパックは次回起動時に`world/datapacks/summering_advancements/`として
書き出されます。

### レア度専用テーブルで "UR" という文字列を直接使わないように
実績「ウルトラレアハンター」の判定が、これまで`"UR"`という文字列をハードコードして
チェックしていました。生物ごとのレア度テーブルの**最後の（最もレアな）項目のキー**を
動的に見て判定するように変更し、コード上で特定のレア度名に依存しない形にしました。
（そのため、生物ごとの`rarities`を書く場合も、共通テーブルと同じキー名の並びを保ってください
＝一番レアな段階が、そのテーブルの最後の項目になるようにしてください。）

### 「浮きに寄ってくる」判定を、密着＋分かりやすい音に変更
`arrival-distance`の既定値を`1.2` → `0.35`に変更し、浮きにほぼ密着するまで「振るタイミング」
が来ないようにしました。密着した瞬間の音量・パーティクル量も増やし、はっきり分かるように
しています。

### 釣りに失敗したときの演出を「煙＋瞬間移動」に統一
`RELOCATE`（既定）モードを、AIを戻して黙って水辺へ移動させる従来の挙動から、
**逃げる前の場所と、逃げた後の場所の両方に煙のパーティクル＋テレポート音を出したうえで、
瞬間的にテレポートする**形に変更しました。移動先は必ず近くの「水の中」に限定し、
見つからなければその場に留まります（水以外の場所へは移動しません）。

### 複数の魚がいる場合、それぞれが独立して判定するように
これまでは「範囲内で一番近い1匹」だけを見て判定していたため、複数匹いても実質1匹ぶんの
確率にしかなっていませんでした。範囲内にいる対象個体**それぞれ**が独立に`trigger-chance`で
判定されるように変更し、複数匹いれば1匹だけの場合より反応しやすくなります（誰かが反応したら
その中から1匹選んで誘導を始めます）。デバッグログ（`debug: true`）にも、判定した匹数と
反応した匹数を出すようにしました。

### そのほか見つけて直した点
- `NetListener`・`FishingListener`・`DexService`・`ApproachFishingService`の一部で、
  レア度名の表示に生物ごとの専用テーブルではなく共通テーブルを見ていた箇所が残っていたのを
  修正しました（表示のズレにつながる可能性があったため）。

---

## 11. v0.5.0: 逃走の仕組み統一、レア度の下限、summering統一の追い込み

### 虫も魚も、逃げるときは煙＋瞬間移動に統一
これまで「逃げる」演出があったのは寄ってくる釣り（FISH_APPROACH）のタイミング判定だけでした。
今回、**虫取り網（虫）・通常釣り（FISH_ROD）・寄ってくる釣り（判定成功後も）すべてに、
共通の逃走判定を追加**しました。

- 虫取り網で捕まえそこねた虫は、煙のパーティクルとともに近くへ瞬間移動します
  （新しい共通ユーティリティ `EscapeEffects` を追加）。
- 通常釣りも、「アタリはあったのに逃げられる」ことがあるようになりました。この場合は
  バニラの釣果を取り消し、浮きの位置に煙エフェクトだけ出します（実体となる個体がないため）。
- 寄ってくる釣りは、タイミングが合っていても最終判定で逃げられることがあるようにしました
  （既存の「煙＋水中へ瞬間移動」の演出をそのまま再利用しています）。

### 逃走確率の決め方（網の捕まえやすさ→生物の逃げやすさ＋道具の補正、に変更）
これまでの「網の catch-chance（捕獲成功率）」を廃止し、以下の新しい仕組みに置き換えました。

- **生物ごとの基準の逃げやすさ**（`creatures.yml` の `escape-chance`、0.0〜1.0）を追加しました。
  省略した場合は `config.yml` の `catching.default-escape-chance`（既定 0.2）が使われます。
- **道具（網・竿）の `escape-modifier`**（符号付き）が、生物の基準に加算されます。
  マイナスなら「逃しにくくなる道具」、プラスなら「逃しやすくなる道具」（特殊アイテム用）になります。
  最終確率は 0〜1 にクランプされるので、基準を `-1` や `2` のような極端な値にしておけば、
  実質「絶対に逃げない／絶対に逃げる」を表現できます。
- ご提示いただいた例（蝶の基準0.5、めいじんの虫取り網の補正-0.3 → 最終20%）に合わせて、
  `nets`/`rods` の既定値・`agehacho`（アゲハ蝶）の`escape-chance`を設定しています。

このため、`nets` の `catch-chance` フィールドは廃止し、代わりに `escape-modifier` を
使うようになりました（設定ファイルの互換性はありません。`catch-chance` は無視されず
単純に存在しない扱いになるので、`escape-modifier` を書き直してください）。

### サイズ連動のレア度下限
`config.yml` に `size-rarity-floor` セクションを追加しました。サイズ段階が「ふつう」から
遠い（極端な）ほど、レア度の下限を引き上げます。例のデフォルトでは、中心から距離2
（とてもちいさい/とてもおおきい）で `R` 以上、距離3（最もちいさい/最もおおきい）で `SR` 以上を
保証します。**最高レア度（テーブルの一番最後の項目、既定では `UR`）は、この仕組みからは
絶対に到達しません** — コード側で「最後の項目のひとつ手前まで」に強制的にクランプしています。
将来、特殊な無条件実装を追加する余地として空けてあります。

この判定は `CatchService.applyBonus()` の中で行っているため、虫・通常釣り・寄ってくる釣りの
どの経路でも自動的に効きます。

**補足（範囲の確認）**：今回の実装は、あくまで「このサイズ連動の仕組み単体では最高レア度に
届かない」という制約です。素の抽選（`rarities`のweight）や、竿・網のレア度ボーナスによる
通常の引き上げでは、これまでどおり最高レア度（`UR`など、重みが0でなければ）が出ることが
あります。もし「特殊な無条件実装ができるまでは、いかなる方法でも最高レア度を一切出さない」
という趣旨であれば、それは今回のスコープを超えるため、別途ご相談ください。

### summering統一の追い込み
`creatures.yml`・`config.yml` に残っていた `item.model` / `guide.model` の
`mushitori:xxx` 表記を `summering:xxx` に修正しました（リソースパック側も summering
名前空間とのことでしたので、合わせました）。

### レア度のUR文字列ハードコード除去（再掲・確認）
前回すでに対応済みですが、`ur_hunter`実績の判定は「テーブルの最後の項目」を動的に見る
形になっています。今回追加した `size-rarity-floor` の「最上位には到達させない」処理も、
同じく `"UR"` という文字列を直接使わず、テーブルの並び順（最後の項目）だけで判定しています。

---

## 12. v0.6.0: レア度の決め方を全面再設計（固定ベースレア度＋サイズ連動）

前回実装した「サイズ連動のレア度下限」をベースに、ご提示いただいた仕様に合わせて
レア度の決め方そのものを作り直しました。確認いただいた3点の回答を踏まえています。

### 変更内容
- **レア度のランダム抽選を廃止**しました。生物ごとに固定の `base-rarity`
  （例: アゲハ蝶=N、カブトムシ=R）を持たせ、そこから最終サイズ段階に応じて
  段階的に繰り上げる方式に変更しています。
- **rare-bonus（竿・網のレア度ボーナス）を廃止**しました。レア度はサイズだけで
  決まります（`GearRegistry.Gear`から`rareBonus`フィールドを削除、config.ymlの
  `rods`から`rare-bonus`、`fishing.approach`から`extra-rare-bonus`を削除）。
- 各生物の`rarities`（重み抽選テーブル）欄は廃止しました。`creatures.yml`は
  `base-rarity: <キー>`の単一値に置き換えています。
- **サイズ→レア度の繰り上げルールを「テンプレート」化**しました。`config.yml`の
  `size-rarity-templates`セクションで複数定義でき、生物ごとに
  `size-rarity-template: <名前>`で選べます（省略時は
  `catching.default-size-rarity-template`）。デフォルトの`default`テンプレートは、
  「ふつう／ちいさい・おおきい」で0段階、「とても〜」で+1段階、「最も〜」で+2段階
  繰り上げます。穏やかな例として`gentle`テンプレートも用意し、カナブンに適用しています。
- **最高レア度への到達条件**：テンプレートの`top-tier-min-base`（ベースレア度がこの
  キー以上）と`top-tier-requires-max-distance`（「最も〜」のときだけ、の意）を
  満たした場合のみ、計算上最高レア度に届いても実際に到達させます。満たさない場合は
  ひとつ下の段階に留めます。
  - 例：カブトムシ（ベース=R）が「最もちいさい/最もおおきい」になると、
    でんせつ（UR）まで到達しえます。
  - 例：アゲハ蝶（ベース=N）は、「最も〜」でも2段階しか繰り上がらないため、
    最大でも「すごくめずらしい」までです（でんせつには届きません）。
  - 例：ベースが「すごくめずらしい」の生物が「とても〜」（+1段階）になった場合、
    計算上はでんせつに届きますが、「最も〜」の条件を満たさないため
    「すごくめずらしい」のまま留まります。
- **「最も〜」の実測サイズ**は、必ず`size-min`/`size-max`ちょうどになるようにしました
  （道具の補正があっても、これを超えることはありません）。

### 補足
- `rarities`の`weight`/`size-bias`は現在の仕組みでは使われていません
  （将来のために残してありますが、並び順だけが重要です）。
- レア度テーブルの並び順（低レア→高レア）は、今回の仕組みの前提として重要です。
  `config.yml`の`rarities`セクションの記載順を変えないようご注意ください。

---

## 13. v0.6.1: 空中クリック・耐久値・進捗反映の再修正

### 虫取り網が空中で発動しない（材質を変えても直らなかった件）
`CARROT_ON_A_STICK`（特殊挙動あり）でも`STICK`（特殊挙動なし）でも同じ症状が出た
ことから、素材固有の問題という仮説を撤回しました。代わりに、`onSwing`の
`@EventHandler(ignoreCancelled = true)`が原因である可能性に着目し、`ignoreCancelled`を
外しました。何らかの理由で他にイベントが打ち消されていても、網を持っているときの
右クリックは必ず処理されるようになります。

あわせて、`net-catching.debug: true`で、網を持っているときに発生した
`PlayerInteractEvent`の生ログ（action・打ち消し済みかどうか）をコンソールに出せる
ようにしました。もしこれでも直らない場合は、`debug: true`にしてブロックに向けた
ときと空中に向けたときのログの違いを見せてもらえると、原因の切り分けがかなり
正確になります（ログの行自体が出ない場合、サーバー側にイベントが届いていない
ということなので、より深い調査が必要になります）。

### 耐久値が変更されない件
`STICK`はバニラ本来「耐久値を持たない」素材で、環境によっては`ItemMeta`が
`Damageable`として扱われず、耐久値の設定（`setMaxDamage`/`setDamage`）自体が
無視されていた可能性があります。虫取り網の既定素材を、バニラで最初から耐久値を持つ
`WOODEN_SWORD`（右クリックに特殊な挙動を持たない道具）に変更しました
（`config.yml`の`nets`、`GearRegistry`のフォールバック値の両方）。

### 進捗が反映されない件
現状の仕組みでは、進捗データパックが実際に読み込まれていないと
`Bukkit.getAdvancement()`が`null`を返し、**何のエラーも出さずに黙って失敗する**
作りになっていました。以下を追加しています。
- `AdvancementService#grant`が進捗を見つけられなかったとき、起動ごとに1回だけ
  警告ログを出すようにしました（`/datapack list`の確認と手動`/datapack enable`を促します）。
- データパック有効化コマンドを叩くタイミングを、起動1秒後→5秒後に遅らせました
  （ワールドの初期化がまだ終わっていないタイミングで叩いていた可能性があるため）。
- `/mushitori reload`を実行すると、設定の再読み込みに加えて進捗データパックの
  再設置・再有効化も試みるようにしました（サーバー再起動なしで再試行できます）。

**お手数ですが、以下を確認していただけますか：**
1. サーバーのコンソールログに「進捗データパックを書き出しました」「進捗データパックを
   有効化します」という行が出ているか
2. ゲーム内で`/datapack list`を実行し、`file/summering_advancements`が
   enabled一覧に入っているか（入っていなければ、手動で
   `/datapack enable "file/summering_advancements"`を実行してみてください）
3. 以前のバージョンで`world/datapacks/mushitori_advancements/`フォルダが
   作成されていた場合、混乱を避けるため手動で削除することをおすすめします
   （名前空間をsummeringに統一したため、今は使われていません）

---

## 14. v0.6.2: 進捗データパックが認識されない問題の修正

空中クリックの修正は成功、とのことでよかったです。

### 原因
Minecraftは、サーバー起動時に一度だけ`datapacks/`フォルダをスキャンしており、
起動後にプラグインがファイルを書き足しても、**明示的に再スキャンしない限り
「存在する」ことすら認識しません**。これまでのコードは、ファイルを書き出した直後に
いきなり`/datapack enable`を叩いていましたが、この「再スキャン」の手順が
抜けていました。`/datapack list`にすら出てこなかったのは、これが原因です。

### 修正内容
データパックのファイルを書き出したあと、
1. `minecraft:reload`（バニラのデータ再読み込みコマンド）を実行して再スキャンさせる
   （単に`reload`と呼ぶとBukkit側のプラグインリロードと名前が衝突するため、
   `minecraft:`を明示的に付けてバニラ側を指定しています）
2. 少し待ってから `datapack enable "file/summering_advancements"` を実行する

という2段階にしました。

### 確認をお願いしたい点
`/datapack list`で探すパック名が、**`mushitori_advancements`ではなく
`summering_advancements`に変わっています**（v0.4.1で名前空間をsummeringに
統一した際にパック名も変更したため）。前回「mushitori_advancementsすらない」との
ことでしたが、そもそも探す名前が変わっている可能性もあるので、`summering_advancements`
で改めて確認してみてください。

今回の修正後、サーバー起動ログに以下の3行が順番に出ているか確認してもらえますか。
- 「進捗データパックを書き出しました: (パス)」
- 「進捗データパックを認識させるため、データの再読み込みを行います: minecraft:reload」
- 「進捗データパックを有効化します: datapack enable "file/summering_advancements"」

---

## 15. v0.6.3: 細かな調整（浮きの位置・逃走演出・scale属性）

進捗の反映、確認ありがとうございます！手動設置で直ったとのことなので、プラグイン側の
自動設置は「サーバー起動直後はまだ完全に立ち上がりきっていないタイミングで
コマンドを叩いていた」可能性を考え、有効化までの待機時間を1秒→15秒に伸ばしました
（根本原因の確証はまだありませんが、低リスクな改善として入れています）。

### 寄ってくる釣り：浮きに近づきすぎて水面から顔を出す問題
魚の目標地点を、浮きの位置そのものではなく「浮きから少し下（水面下）」にずらしました。
`fishing.approach.approach-depth-offset`（既定0.4ブロック）で調整できます。

### 逃走演出：もう少し遠くへ／テレポートの軌跡を消す
- 虫取り網・通常釣りの逃走テレポート距離を伸ばしました
  （`net-catching.escape-teleport-radius`: 4.0 → 7.0）。
- 寄ってくる釣りの水中への再配置距離も伸ばしました
  （`fishing.approach.escape.relocate-radius`: 6.0 → 9.0）。
- テレポートの瞬間だけ対象を透明化してから移動させ、少し経ってから元に戻すようにしました
  （`EscapeEffects`、`ApproachFishingService`双方）。クライアント側の補間による
  「滑るように見える」動きを隠し、「一瞬で消えて、別の場所に現れる」ように見えるはずです。

### scale属性（見た目の大きさ）
`config.yml`の`catching.scale`セクションで、見た目のスケール（Attribute.GENERIC_SCALE）を
設定できるようにしました。`/mushitori spawn`等で個体が湧いた瞬間、その個体の「素の」
サイズ段階（最もちいさい〜最もおおきい）の位置に応じて、`min`（既定0.5）〜`max`
（既定1.5）の範囲で直線的にスケールを決めます。つまり、捕まえる前の見た目からも
おおよその大きさが伝わるようになります。

**未検証の点**：`Attribute.GENERIC_SCALE`はPaper APIの定数名です。もしお使いの
Paper 26.2でこの定数が変わっている場合（例えば`Attribute.SCALE`など）、コンパイル
エラーになる可能性があります。その場合はエラーメッセージを送ってください。

---

## 16. v0.6.4: まとめての細かい調整

### 寄ってくる釣り：逃げた魚のクールダウン
魚が逃げてから`fishing.approach.escape-cooldown-seconds`（既定20秒）が経つまでは、
その個体は「寄ってくる」判定の対象から除外されるようにしました。逃げた直後に
すぐまた同じ魚が寄ってくる、ということがなくなります。

### /kill コマンドで虫・魚が消せない問題
`NetListener`の当たり判定無効化（`EntityDamageEvent`を常に打ち消す処理）が、
`/kill`コマンドによるダメージまで巻き込んで防いでいたのが原因でした。
`DamageCause.KILL`のときは素通りするようにし、`/kill`で削除できるようにしました。

### 熱帯魚（TROPICAL_FISH）の柄・色を指定
`creatures.yml`の`tropical-fish`セクションで、`pattern`（模様）・`body-color`（体色）・
`pattern-color`（模様色）を指定できるようにしました。省略した項目はバニラの
ランダムな見た目のままです。ニシキゴイに設定例を入れています。

### 「いつもの」の表示色（白に見えていた問題）
原因が判明しました。`config.yml`のレア度色指定が`light_gray`になっていましたが、
これはMinecraftの文字色として存在しない名前です（DyeColorの命名と混同しやすい
ところです）。無効な色名は解釈できずに白へフォールバックする作りだったため、
実質的に白として表示されていました。正しいキー名`gray`に修正しています。
（使える16色をconfig.ymlにコメントで明記しました）

### アイテム説明文のレア度表記・即時通知の見直し
- 説明文のレア度行を「めずらしさ：いつもの」のような表記に変更しました。
- 虫取り網・通常釣り・寄ってくる釣り、いずれも捕獲直後のアクションバー表示から
  レア度を外しました（名前とサイズだけ表示し、レア度はアイテムの説明文や
  図鑑で確認する形にしています）。

### 図鑑：レア度・サイズを「捕まえた回数」で表示
これまでは「捕まえたことがあるかどうか」だけの表示でしたが、`PlayerDex.Entry`を
Set（有無のみ）からMap（レア度・サイズ段階ごとの捕獲数）に変更し、図鑑の説明文に
「サイズ段階ごとの捕獲数」「レア度ごとの捕獲数」として、それぞれの実際の捕獲数を
表示するようにしました（0回のものは表示されません）。

**注意（互換性）**：`PlayerDex`の保存形式が変わっています（`rarities`/`size-tiers`が
リストからマップに変わりました）。過去のセーブデータの該当項目は読み込めず、
その部分は空から再スタートします（捕獲数・最大/最小サイズ・実績等、他の項目は
そのまま引き継がれます）。

---

## 17. v0.6.5: コンパイルエラー修正

エラーログありがとうございます。2箇所直しました。

- `Attribute.GENERIC_SCALE`という定数は存在しませんでした。正しくは`Attribute.SCALE`
  でした（`CatchService.java`）。
- `EntityScheduler#runDelayed`は`(Plugin, Consumer<ScheduledTask>, Runnable, long)`の
  4引数が必要でしたが、3引数（`Runnable`の部分を省略）で呼んでいました。
  該当の2箇所（`EscapeEffects.java`、`ApproachFishingService.java`）とも、
  `Runnable`の引数に`null`を渡す形に直しました。

なお、ビルドログに出ていた`Sound.valueOf(String)`が非推奨という警告（2箇所）は、
今回は直していません。ビルドを止めるエラーではなく、動作にも今のところ影響しない
想定ですが、将来のバージョンで削除される可能性はあります。対応が必要であれば
お知らせください。

---

## 18. v0.7.0: config補完・生物ごとのベースscale・リセットコマンド・メッセージ外部化

### /mushitori reload でデバッグ設定が反映されない件
コード自体に明確な誤りは見当たりませんでしたが、これまで何度もconfig.ymlに項目を
追加してきたため、**古いバージョンから使い続けているサーバー側のconfig.ymlに
新しい項目が反映されていない**可能性を考えました。`saveDefaultConfig()`は
ファイルがすでに存在すると何もしないため、アップデートのたびに増えた項目
（`net-catching.debug`等）が、ユーザーの実ファイルには存在しないままになりえます。

対策として、起動時・`/mushitori reload`時の両方で、**同梱のconfig.ymlを「既定値」として
セット**するようにしました（`FileConfiguration#setDefaults`）。ユーザーの実ファイルは
書き換えませんが、実ファイルに無い項目は同梱の既定値で自動的に補われます。
これで、config.ymlを手動で最新化しなくても新項目が効くようになるはずです。

### 生物ごとのベースscale
`creatures.yml`に`base-scale`（既定1.0）を追加しました。実際に適用されるscaleは
「`base-scale` × サイズ段階による倍率（config.ymlのcatching.scale、既定0.5〜1.5）」に
なります。例としてアゲハ蝶（entity: BEE）に`base-scale: 0.7`を設定し、本来のハチより
一回り小さい見た目を基準にしています。

### イワナ（SALMON）でscaleが変化しない件
コード上、生物の種類によってscale適用処理を分けている箇所はないため、
処理そのものに差は無いはずです。考えられるのは、entityの種類によっては
そもそも`Attribute.SCALE`を持たない（Minecraft側の制約）可能性です。
`catching.debug: true`にすると、個体を湧かせるたびに
「scale適用: ○○ (ENTITY_TYPE) base-scale=... tierFactor=... -> 最終値」または
「scale未適用（このentity種別はAttribute.SCALEを持っていません）」のどちらかが
ログに出るようにしたので、次回イワナを湧かせて実際にどちらのログが出るか
確認してもらえると、原因を確定できます。

### /kill と /mushitori reset
- 前回対応した`/kill`（`DamageCause.KILL`除外）は引き続き有効です。
- 図鑑・実績を初期化する`/mushitori reset <プレイヤー> [dex|achievements|all]`
  （既定`all`）を追加しました。`dex`は図鑑の記録のみ、`achievements`は実績のみ、
  `all`は両方をリセットします。

### 直接釣った際（寄ってくる釣り）の失敗時に何も表示されない件
原因が分かりました。寄ってくる釣りで「タイミングは合っていたが、最後の判定で
逃げられた」場合（`onReelAttempt`内の`rollEscape`分岐）、内部ログだけ出していて、
プレイヤーへの通知が実装されていませんでした。通常釣り側にはすでにあった
「あと一歩のところで、魚に逃げられた…」に相当するメッセージを、寄ってくる釣り側にも
追加しました（`{creature}`が魚の名前に置き換わります）。

### 通知文言のJSON外部化
`messages.json`（プラグインのデータフォルダ直下）と、それを読み込む`MessageService`を
新設しました。捕獲・釣りの通知文言をここで編集できます（`{creature}`・`{size}`・
`{count}`のプレースホルダーが使えます）。編集後は`/mushitori reload`で反映されます。
対象化した文言は以下の6種類です。

- `net.catch.single` / `net.catch.multiple`（虫取り網）
- `fishing.rod.catch` / `fishing.rod.escape`（通常釣り）
- `fishing.approach.catch` / `fishing.approach.escape`（寄ってくる釣り。今回追加した文言もここに含まれます）

**未検証の点**：`messages.json`の読み込みに`com.google.gson.Gson`を使っています。
Paper APIの依存関係経由で標準的に使えるはずのライブラリですが、コンパイル時に
見つからない場合はお知らせください。

---

## 19. v0.7.1: scale計算式をご提示いただいた形に変更

これまでの「サイズ段階の位置に応じて0.5〜1.5倍の範囲で直線的に変化」という方式をやめ、
ご提示いただいた計算式に変更しました。

```
平均サイズ = (size-min + size-max) / 2
scale = (実測cm ÷ 平均サイズ) × base-scale
```

さらに、「最もおおきい」段階のときは`extreme-max-multiplier`（既定1.1）を、
「最もちいさい」段階のときは`extreme-min-multiplier`（既定0.9）を最後に掛けます。

例（アゲハ蝶: size-min=7, size-max=12, base-scale=0.7）：
- 「最もおおきい」（実測cm=12）: 12 / ((7+12)/2) × 0.7 × 1.1 ≈ 0.97
- 「ふつう」あたり（実測cm≒9.5）: 9.5 / 9.5 × 0.7 = 0.7
- 「最もちいさい」（実測cm=7）: 7 / 9.5 × 0.7 × 0.9 ≈ 0.46

生物ごとの`size-min`/`size-max`の比率に応じたスケールになるため、旧方式のように
段階だけで一律0.5〜1.5倍になることがなくなり、鮭（イワナ）のように極端な変化を
感じていたケースも緩和されるはずです。`config.yml`の`catching.scale`セクションを
`min`/`max`から`extreme-max-multiplier`/`extreme-min-multiplier`に置き換えています
（**設定キーの互換性はありません**。既存の`min`/`max`は無視されます）。

`catching.debug: true`のログにも、実測cm・平均サイズ・比率・最終scaleを出すように
更新したので、引き続き挙動の確認にお使いください。

---

## 20. v0.8.0: 魚の獲得方法統一・お金のアイテム化・虫かご 他

### 魚の獲得方法を統一
`Category.isFish()`（FISH_ROD / FISH_APPROACHをまとめて判定）を活用し、
`CreatureRegistry.fishCreatures()`を新設しました。通常釣り（`FishingListener`）・
寄ってくる釣り（`ApproachFishingService`）のどちらも、カテゴリを問わず**全ての魚**を
対象にするよう変更しています。カテゴリ自体は図鑑タブ・実績の整理用として残しています。

ご要望にあった「釣る位置（バイオーム）によってloot_tableを変える」仕組みは、
今回は実装していません（「将来的に」とのことでしたので）。現状、`fishCreatures()`から
さらに絞り込むような形で後付けしやすい構造にはなっています。

### お金をアイテムで管理
`MoneyService`を全面書き直しました。スコアボードではなく、プレイヤーの持ち物にある
「所持金コイン」アイテムのスタック数で所持金を管理します（`config.yml`の`money`
セクションで見た目を設定）。図鑑への表示は特にしていません。銀行のような仕組みは
構想段階とのことで、今回は対応していません。

**注意（互換性）**：これまでスコアボードに貯めていた所持金は引き継がれません
（新方式では0からのスタートになります）。必要であれば、移行用のコマンドを
別途ご用意できます。

### hidename チームへの自動参加
虫・魚を湧かせた瞬間（`SpawnService`）に、既存の"hidename"という名前のスコアボード
チームがあれば自動的に参加させるようにしました（チームが無ければ何もしません）。

### サイズ表示を小数点以下2桁に
`CatchService.formatSize`をはじめ、サイズの丸め処理をすべて小数点以下2桁
（`%.2f`、0埋め込み）に変更しました。

### 虫かごを追加
新規`CageService`・`CageGui`・`CageListener`を作成しました。
- 手持ちの状態で右クリックするとGUIが開きます（エンダーチェストやシュルカーボックスと
  同じ操作感）。閉じると中身がアイテム自身に保存され、そのまま他のプレイヤーに渡せます。
- 虫（BUGカテゴリ）以外は上段（虫かご側）に置けません。
- `config.yml`の`cages`セクションでtierごとに容量・素材・価格を設定できます
  （既定: tier1=5, tier2=10, tier3=20）。
- `/mushitori give cage [tier]`で入手できます。

**未検証の点（3つ）**：今回使用したAPIのうち、以下は確証が持てていません。
コンパイルエラーが出た場合はエラーメッセージを送ってください。
- `ItemMeta#setMaxStackSize(int)`（所持金コインのスタック上限を引き上げる用途）
- `ItemStack#serializeAsBytes()` / `ItemStack.deserializeBytes(byte[])`
  （虫かごの中身をNBTごと保存・復元する用途）
- `PersistentDataType.TAG_CONTAINER`（虫かごの中身を入れ子のPDCとして保存する用途。
  こちらは比較的古くからあるAPIのため、他の2つよりは確度が高いと見ています）

---

## 21. v0.9.0: 進捗のリセット連動・図鑑アイテムの素材変更・アンビエントスポーン

### 個人データのリセット時に、Minecraft本体の進捗も連動
`AdvancementService`に`revoke()`/`revokeAll()`を追加しました。`/mushitori reset`で
`achievements`または`all`（既定）を指定した場合、図鑑側の実績記録と合わせて、
Minecraft本体の進捗（進捗タブに表示されているもの）もあわせて取り消すようにしました。

### 図鑑アイテムの素材をKNOWLEDGE_BOOKに変更
図鑑を持って右クリックで開く機能自体は、元のソースからすでに実装されていたものです
（`GuideListener`）。ベースの素材を`BOOK`から`KNOWLEDGE_BOOK`に変更しました
（`config.yml`の`guide.material`、`GearRegistry`のフォールバック値の両方）。
KNOWLEDGE_BOOKはバニラ側で特殊な用途がほぼ無いため、誤操作が起きにくいという利点があります。

### 【新規】アンビエントスポーン（マーカーによる自動湧き）
将来のマップ探索ゲームに向けた土台として、マーカーを設置しておくと、プレイヤーが
近づいたときだけ自動でいきものが湧く仕組みを追加しました。

**採用した設計**（ご提案いただいた案をそのまま採用しています）：
- マーカー（位置＋生物種＋範囲）を`/mushitori marker add <いきものID>`で設置
  （現在地に設置。`spawn_markers.yml`に保存され、`/mushitori marker remove <ID>` /
  `/mushitori marker list`で管理できます）
- 常時湧いているのではなく、プレイヤーが`trigger-radius`（既定15ブロック）に
  近づいたときだけ湧くかどうかを判定します
- **その場所で直近捕まえたばかりのプレイヤーだけ**が近くにいる場合は湧きません
  （`player-cooldown-seconds`、既定300秒）
- 未経験、またはクールダウンが切れたプレイヤーが1人でも近くにいれば、その場で
  即座に湧きます（1人が居座って独占するのを防ぐ狙いです）
- 湧いた後、`despawn-radius`（既定20ブロック）内に誰もいなくなると自動でデスポーンします

**ラグへの配慮**：
- グローバルで毎tick全マーカーを見て回るのではなく、**マーカーごとに個別の
  リージョンスケジューラタスク**を立て、`scan-interval-ticks`（既定100tick＝5秒）
  おきにだけ、そのマーカー1つぶんの状態確認をします。マーカー数が増えても、
  1回の処理は常に軽量なままです。
- 湧いている個体がいる間は、新たな湧き判定自体を行いません（無駄な判定を省略）。
- サーバー再起動をまたいでも、既にそのマーカー由来の個体が実在すれば
  二重に湧かせないよう、起動時に照合する処理を入れています。

**実装の再利用**：実際に湧かせる部分は既存の`SpawnService.spawn()`（`/mushitori spawn`と
同じ処理）をそのまま呼び出しているだけなので、特性の焼き付け・scale・
寄ってくる釣りとの連携（マーカー由来の魚も、寄ってくる釣りの対象になります）は
自動的にそのまま効きます。

**今回のスコープ外**（ご要望の「将来的に」の部分）：
- マーカーの範囲や湧く条件をGUIで編集する機能（現状はコマンドのみ）
- 複数のマーカーが密集している場合の総量規制（現状は各マーカーが独立して動作）
- こういった追加が必要であれば、随時ご相談ください。

---

## 22. v0.10.0: マーカーの拡張・虫かご不具合修正・ギアのID管理化

### 虫かごが召喚できない不具合
原因は、ユーザーのconfig.ymlに`cages`セクションが丸ごと存在しない場合、
`getConfigurationSection()`が同梱の既定値をうまく拾えていなかったことでした。
`applyConfigDefaults()`を強化し、丸ごと欠けているトップレベルのセクションは、
メモリ上の設定に直接コピーするようにしました（ファイル自体への書き込みはしません）。
これで、新しく追加したセクションが古いconfig.ymlに存在しなくても、実データとして
確実に認識されるはずです。

### マーカー：範囲内のランダムな位置に湧くように
`SpawnMarker`に`spawn-radius`を追加しました。マーカーの中心ちょうどではなく、
その範囲内のランダムな地点に湧きます（`ambient-spawn.spawn-radius`、既定5ブロック。
マーカーごとに`spawn_markers.yml`で個別に上書きも可能）。

### マーカー：複数種類・最大数（プリセット）
`config.yml`の`ambient-spawn.presets`で、複数の生物種＋同時に存在できる最大数を
まとめて定義できるようにしました（`SpawnPreset`）。
```yaml
presets:
  forest_bugs:
    creatures: [agehacho, kabutomushi, kanabun]
    max-count: 3
```
`/mushitori marker addpreset <プリセット名>`で、そのマーカーは複数種類からランダムに、
最大数まで同時に湧くようになります（従来の単一種指定`/mushitori marker add <ID>`も
引き続き使えます）。内部的にはマーカーごとに複数体のUUIDを追跡する形に変更しています。

### マーカー設定棒（視覚的な設定用デバッグアイテム）
`/mushitori give wand`で入手できます。
- **右クリック**：狙ったブロックの上に、選択中の種類でマーカーを設置
- **左クリック**：自分の近くにある一番近いマーカーを削除
- **スニーク＋右クリック**：設置する種類（生物 / プリセット）を順番に切り替え
- 持っている間、近くのマーカー（32ブロック以内）がパーティクルで見えるようになります
  （中心に光の粒、spawn-radiusの範囲を円で表示）

### 虫取り網・釣竿をIDで管理
`config.yml`の`nets`/`rods`セクションのキーを、tier番号（1, 2, 3）から文字列ID
（`my_net`, `new_net`, `master_net` / `my_rod`, `new_rod`, `master_rod`）に変更しました。
`GearRegistry`・`Keys`（`NET_TIER`→`NET_ID`、`ROD_TIER`→`ROD_ID`、値もINTEGERから
STRINGへ）・`/mushitori give net|rod <ID>`すべて対応しています。

**注意（互換性）**：以下は破壊的変更です。
- 既存の虫取り網・釣竿アイテム（tier番号で焼き付けられたもの）は、更新後は
  認識されなくなります（`/mushitori give`で作り直してください）。
- config.ymlの`nets`/`rods`セクションを、上記の新しいキー形式に書き換える必要があります
  （このzipのconfig.ymlはすでに新形式になっています。手動で編集していた場合は
  差分を確認してください）。

なお、ついでに`rods`のtier3（名人の釣竿）で見つけた`model`のtypo
（`summering_master_rod` → 正しくは`summering:master_rod`、コロン抜け）も修正しています。

---

## 23. v0.11.0: マーカーをGUIで直感的に編集できるように

### マーカーは、設置した瞬間から「自分専用の値」を持つように変更
前回まで、マーカーの半径等は「未指定ならグローバル既定値を使う」という上書き方式
でしたが、今回から**設置した瞬間にその時点の既定値（またはプリセットの内容）を
コピーして持つ**方式に変更しました。プリセットは「複数のマーカーで設定を使い回す
共有オブジェクト」から「新規設置時の便利なテンプレート」に役割が変わっています。
これにより、設定画面でマーカーごとに個別編集できるようになっています。

### マーカー設定画面（GUI）
設定棒（`/mushitori give wand`）で**既存のマーカーを右クリック**すると、そのマーカー
専用の設定画面が開くようになりました（`MarkerGui`）。
- 生物欄（最大5種類）：左クリックで生物を切り替え、右クリックで空欄に
- 感知範囲・デスポーン範囲・湧く範囲・最大数・湧く頻度（秒）・カウント窓（秒）：
  それぞれ左クリック+1／右クリック-1／シフト+クリックで×10
- 「プリセットを適用」：クリックのたびに次のプリセットを適用（生物一覧と最大数を上書き）
- 「このマーカーを削除」：即座に削除

画面を閉じると、変更内容がまとめて確定されます（湧く頻度を変えた場合は、その
マーカーの巡回タスクを自動的に再起動します）。設定棒での操作方法も変わりました：
既存マーカーが近くになければ新規設置、あれば設定画面を開きます（右クリック共通）。

### 最大数の判定方法を「常時キープ」から「直近の捕獲実績ベースの補充」に変更
これまでは「今何体生きているか」だけを見ていましたが、ご要望に合わせて
「現在の生存数 ＋ 直近 catch-window-seconds 秒以内にそこで捕まえられた数」が
max-count を下回っている間、spawn-interval-seconds 秒おきに1体ずつ補充される
方式に変更しました。誰も捕まえていなければ生存数だけで頭打ちになり、捕まえた分は
時間経過（カウント窓を過ぎる）とともにまた補充される、という動きになります。

**注意（互換性）**：`spawn_markers.yml`の保存形式が変わっています（`preset`参照から
`creatures`の直接リストへ）。過去に設置したマーカーは、次回起動時に読み込めない
可能性があります。お手数ですが、設定棒で置き直していただくのが確実です。

---

## 24. v0.11.1: 虫かごをIDで管理・出し入れの不具合修正

### 虫かごをtier番号ではなく文字列IDで管理
`config.yml`の`cages`セクションを、`1/2/3`から`small_cage`/`cage`/`large_cage`に
変更しました。`CageService`・`Keys`（`CAGE_TIER`→`CAGE_ID`、INTEGERからSTRINGへ）・
`/mushitori give cage <ID>`すべて対応しています。

**注意（互換性）**：既存の虫かごアイテムは、更新後は認識されなくなります
（`/mushitori give cage <ID>`で作り直してください）。

### 出し入れの不具合（虫以外を弾く判定・容量オーバー）
これまでは、クリックの種類（通常クリック／シフトクリック等）ごとに個別に
「置こうとしているアイテムが虫かどうか」を判定してキャンセルする作りでしたが、
数字キーでの入れ替えやダブルクリックでの一括回収など、判定漏れが起きうる
クリック種別が他にもありました。

判定を全面的に見直し、**クリック・ドラッグの処理自体はバニラに任せたうえで、
1tick後に中身全体を検証し、条件を満たさない分だけ取り除いてプレイヤーへ返す**
方式に変更しました。クリック種別ごとの違いを個別に想定する必要がなくなるため、
より確実に「虫以外は入らない」「容量を超えない」を強制できるはずです。

あわせて、容量が9の倍数でない場合（例: 容量5）にGUIのスロット数が9の倍数に
切り上げられ、余ったスロットに何の制限もかかっていなかった点も修正しました。
容量を超えるスロットは、灰色のガラス板（使用不可の目印）で塞がれ、実際に
使えるのは設定した容量ぶんだけになります。

---

## 25. v0.11.2: 虫かごの容量をGUIの段数に合わせる

`cages`の既定容量を、GUIの行数（9マス単位）にちょうど合うよう
5/10/20 → **9/18/27**（1段／2段／3段）に変更しました。これにより、余りの
使用不可スロットが出ない、すっきりした表示になります。

`/mushitori give cage`は、前回のzip（v0.11.1）の時点ですでに文字列ID対応
（`small_cage` / `cage` / `large_cage`）になっています。もし引き続き
tier番号（1〜3）の挙動が出る場合は、お手数ですが今回のzipで入れ替えて、
`/mushitori give cage small_cage`のようにID名を指定する形で試してみてください。

---

## 26. v0.12.0: creatures.ymlをいただいた内容に更新・魚のbase-scale・FISHカテゴリ統合

### creatures.yml を、いただいた内容に更新
アゲハ蝶を`fly`（ナミアゲハ）に改名、`yellow_fly`（キアゲハ）を新規追加した内容を、
そのままデフォルトのcreatures.ymlとして採用しました。

### 魚のbase-scale
これまで虫（蝶）にしか設定していなかった`base-scale`を、魚（マダイ・アジ・ニシキゴイ・
イワナ）にも追加しました（いずれも1.0）。`base-scale`が未設定だと既定値1.0が使われる
ため、機能自体は元から魚にも使えていましたが、明示的に設定していなかったことで
気づきにくくなっていました。値は1.0のまま置いていますので、実際の見た目を確認しつつ、
必要であれば種ごとに調整してください。

なお、以前ご報告いただいた「イワナ（SALMON）でサイズによってscaleが変化しない」件は、
今回の変更だけでは解決しない可能性があります。`catching.debug: true`にした状態で
イワナを湧かせたときのログ（scale適用 / scale未適用のどちらが出るか）を、もしまだ
確認できていなければ、そちらも見てもらえると原因を切り分けられます。

### FISH_ROD / FISH_APPROACH を FISH に統合
ご指摘の通り、以前「魚の獲得方法を統一」した時点で、FISH_ROD/FISH_APPROACHという
2つのカテゴリを分けておく意味は実質的に無くなっていました。今回、`Category`列挙型
そのものを`BUG`/`FISH`の2つに統合しました。

- `creatures.yml`の`category`は、魚はすべて`FISH`に変更（マダイ・アジ・ニシキゴイ・
  イワナ全て）。
- 実績「さかな（釣りざお）コンプリート」「さかな（よってくる）コンプリート」の2つは、
  「さかな コンプリート」1つに統合されました（実績IDは`Category`の値から自動生成
  されるため、Javaコード側の変更は不要でした）。
- 進捗データパック側も、`complete_fish_rod.json`/`complete_fish_approach.json`を
  廃止し、`complete_fish.json`に統合。`complete_all`の親もこちらに繋ぎ直しています。

**注意（互換性）**：実績IDが変わったため、`complete_fish_rod`/`complete_fish_approach`
を達成済みだったプレイヤーの進捗は、新しい`complete_fish`には自動的に引き継がれません
（図鑑側の登録データ自体は影響を受けません）。

### あじ（アジ）に寄ってくる釣りの個別設定を追加
`approach`セクションを追加し、数が多い魚らしく共通設定よりやや当たりやすくしています
（`patience-seconds: 8.0`, `trigger-chance: 0.40`）。マダイにも同様に追加しました
（`patience-seconds: 12.0`, `trigger-chance: 0.30`）。カテゴリ統合により、この設定は
FISHであればどの生物にも指定できます（以前のようにFISH_APPROACH限定ではありません）。

---

## 27. v0.13.0: マーカーの生物ごとのウェイト設定・最大種類数の拡張

### 生物ごとのウェイト（重み）設定
マーカー・プリセットが持つ生物一覧を、単なるID一覧から「ID＋ウェイト」の一覧
（`WeightedCreature`）に変更しました。ウェイトの比率でランダムに選ばれます。
ご提示の例（キアゲハ7割、ナミアゲハ2割、カブトムシ1割）は、
`{id: yellow_fly, weight: 7}`, `{id: fly, weight: 2}`, `{id: kabutomushi, weight: 1}`
のように設定します（config.ymlの`ambient-spawn.presets`に実例を入れています）。

マーカー設定画面（GUI）の生物枠も操作方法を拡張しました。
- 通常クリック：割り当てる生物を切り替え（ウェイトは維持）
- 右クリック：ウェイト -1
- シフト＋左クリック：ウェイト +1
- シフト＋右クリック：枠を空にする

アイコンには「生物名 (重み N ≒ XX%)」のように、現在の構成での実際の出現割合も
表示されるようにしています。

### マーカーに入れられる最大種類数を拡張
GUIの生物枠を5→9（1行ぶんフル）に増やしました。`SpawnMarker.MAX_SPECIES`で
定数管理しており、9種類まで登録できます。

**注意（互換性）**：`spawn_markers.yml`・config.ymlのプリセットの保存形式が変わって
います（生物一覧が文字列リストから `{id, weight}` のリストへ）。過去のプリセット・
マーカーは、お手数ですが新しい形式で書き直してください（文字列だけのリストも
一応読み込めるようフォールバックは入れていますが、ウェイトは全て1として扱われます）。

---

## 28. v0.13.1: 見た目のサイズと捕獲時の表示サイズが一致しない問題を修正

### 原因
`applyScale`（見た目のscale計算）と、実際に捕まえたときにアイテムへ表示する実測cmが、
**それぞれ独立した乱数で別々に決まっていました**。湧いた瞬間にscale計算用の実測cmを
その場だけ使って抽選し、捕まえる瞬間にはまた別に実測cmを抽選し直していたため、
サイズ段階が変わらない（道具の大物ボーナスが発動しない）場合でも、見た目と
表示サイズが一致しないのが常態化していました。イワナに限らず、scaleが効いている
生物すべてに起きていたはずです。

### 修正内容
湧いた瞬間に決める実測cm（{@code base_size_cm}）を、見た目のscale計算にも、
捕まえたときの表示にも**同じ値として**使うようにしました。

- `bakeTraits`が、サイズ段階だけでなく実測cmもこの時点で1回だけ抽選し、
  エンティティのPDCに焼き付けるようになりました。
- 見た目のscaleは、この焼き付けた実測cmを使って計算します。
- 捕まえる瞬間、道具の大物ボーナスで**サイズ段階が変わらなかった場合**は、
  焼き付けた実測cmをそのまま表示に使います（見た目とラベルが一致します）。
- 道具の大物ボーナスで**サイズ段階が変わった場合**（＝道具の効果でワンランク
  大きく／小さく釣れた場合）だけ、新しいサイズ段階の中から改めて実測cmを
  抽選します（この場合は、見た目より大きく／小さく釣れたように見えるのが
  正しい挙動です。道具の効果が実感できるよう、あえてそのままにしています）。

バニラ釣り（通常釣り・個体が存在しない経路）は、元々「見た目」が無いため、
この問題の対象外です（変更していません）。

---

## 29. v0.14.0: マーカーの湧き方修正・徘徊範囲・名前・設定棒の操作再設計

### 1匹とると湧かなくなる不具合（根本原因が判明）
`player-cooldown-seconds`（そのマーカーで捕まえたプレイヤー自身に対する、再度
「湧きを起こせる側」に戻るまでのクールダウン）が、maxCount/catchWindowによる
補充ロジックより先に効いてしまい、**ソロでテストしていると、捕まえた瞬間に
「近くに適格なプレイヤーがいない」状態になり、以降クールダウンが切れるまで
一切の新規湧き判定がスキップされる**という状態になっていました。最大数が5でも、
実質1匹しか湧けなかった／1匹とると全部消えたように見えたのは、これが原因です。

この仕組みを完全に撤去し、**maxCount ＋ catchWindow だけで湧き方を管理する**形に
一本化しました（`player-cooldown-seconds`の設定項目も削除）。これで、最大数まで
並行して複数体が存在でき、1匹捕まえても残りはそのまま残ります。

### 出現範囲内にとどまるように
湧いた個体が `spawn-radius × 1.3` を超えて離れると、巡回のたびに範囲内の
ランダムな地点へ戻すようにしました（`leashMobs`）。完全な行動範囲制限ではなく
「できるだけ範囲内にとどまる」程度の緩やかな仕組みです。

### マーカーに名前を付けられるように
`SpawnMarker`に`name`を追加しました。設定画面に「名前を変更」ボタンを追加し、
クリックするとバニラの金床と同じ入力欄を持つ画面が開きます（文字を入力して
閉じるだけで確定します）。名前が未設定の場合は「マーカー #ID」と表示されます。

### 設定棒の操作を再設計
- **右クリック**：近くに既存マーカーがあれば設定画面を開く。無ければ、その場に
  「種類未設定」の新規マーカーを設置し、そのまま設定画面を開きます。
- **スニーク＋右クリック**：設置済みマーカーを一覧表示するGUI（`MarkerListGui`）を
  開きます。クリックすると、そのマーカーの場所へテレポートします。
- 以前あった「スニーク＋右クリックで新規設置時の種類を巡回選択する」機能は廃止しました。
  種類の選択は、ご要望の通り設定画面の「プリセットを適用」（本棚アイコン）や、
  生物枠を直接クリックする形に統一しています。

**注意（互換性）**：`spawn_markers.yml`の保存形式は変わっていません（`name`は
省略可能なフィールドとして追加しているため、既存のマーカーもそのまま読み込めるはずです）。

---

## 30. v0.14.1: 範囲外に出た個体を、テレポートではなく歩いて戻すように

`leashMobs`を、瞬間移動ではなく `Mob#getPathfinder()`（バニラのAIナビゲーション）を
使って歩いて戻る方式に変更しました。`ambient-spawn.leash-walk-speed`（既定1.0）で
戻る速さを調整できます。パスファインダーを持たないentity種別（一部の特殊なmob）
への保険として、その場合だけ従来通りの瞬間移動にフォールバックします。

---

## 31. v0.15.0: マーカーの名付け方式変更・湧く確率・設定棒の操作再々設計

### 名付け機能をチャット入力方式に変更
金床（アンビル）方式は`AnvilInventory#getRenameText()`という、確証の持てないAPIに
依存していたのが不調の原因だった可能性があります。より確実な**チャット入力方式**に
切り替えました。設定画面で「名前を変更」をクリックすると画面が閉じ、チャットに
新しい名前を入力するとそのまま反映されます（"cancel"でキャンセル）。

### 湧く確率を追加
`spawn-chance`（既定1.0）を追加しました。湧く頻度（`spawn-interval-seconds`）の
間隔が来るたびに、この確率を判定し、外れた場合は何も起きずに次の間隔でまた
判定します。設定画面にも「湧く確率」のスロットを追加しています
（左クリック±10%、シフト+クリックで±1%の細かい調整）。

### 設定棒の操作を再設計
ご要望に合わせて、右クリックとスニーク＋右クリックの役割を入れ替え、
「選択→移動」の2段階操作にしました。

- **右クリック**：近くの既存マーカーを「選択」して設定画面を開く（近くに無ければ何もしません）
- **スニーク＋右クリック**：直前に右クリックで選択したマーカーがあれば、
  それを今立っている場所へ移動させます。選択中のマーカーが無ければ、
  狙った場所に「種類未設定」の新規マーカーを設置し、選択したうえで設定画面を開きます
- **左クリック**：近くのマーカーを削除（変更なし）
- **スニーク＋左クリック**：マーカー一覧＋テレポート機能（`MarkerListGui`）は、
  こちらに移動しました（以前はスニーク＋右クリックに割り当てていましたが、
  今回その枠が「選択中マーカーの移動」に使われることになったためです）

マーカーを移動した場合、その巡回タスク（Paperのリージョンスケジューラ）も
新しい場所で再起動されるようにしています。

---

## 32. v0.15.1: 虫取り網・釣竿の説明文まわりを整理

### 説明文をconfigで書けるように
`nets`/`rods`各tierに`description`（複数行のリスト）を追加しました。アイテムの
説明文の先頭に表示されます。

### IDの表示を廃止
これまで説明文の1行目に出ていた「虫取り網  master_net」のようなID表示をやめました。

### 「逃げやすさ補正」→「捕まえやすさ」表記に変更
`escape-modifier`は内部的には「逃げやすさへの補正（マイナスほど逃しにくい）」の
ままですが、表示だけ符号を反転させ、「捕まえやすさ」として分かりやすく表示する
ようにしました。例：`escape-modifier: -0.3` → 「捕まえやすさ +30%」。

### 耐久値の行を非表示に
説明文から「耐久値 N」の行を削除しました（設定自体は変わらず、バニラの耐久値
バー表示はそのまま残ります）。

### 剣・道具素材の攻撃力・攻撃速度を非表示に
虫取り網の素材（WOODEN_SWORD）に由来する攻撃力・攻撃速度等の表示を、
`ItemFlag.HIDE_ATTRIBUTES`で隠すようにしました（釣竿にも同様に適用しています）。

---

## 33. v0.16.0: サイズ9段階・レア度5段階（SSR追加）

サイズ段階・レア度テーブルの仕組みは、もともと段数やキー名をコード側に
ハードコードしない設計にしていたため、今回は**config.ymlの調整のみ**で
反映できました（Javaコードの変更はありません）。

### サイズを9段階に
「すごくちいさい」「すごくおおきい」を追加し、ご指定の重みに変更しました。

| 段階 | 重み |
|---|---|
| 最もちいさい | 0.1 |
| すごくちいさい | 1 |
| とてもちいさい | 5 |
| ちいさい | 18 |
| ふつう | 50 |
| おおきい | 18 |
| とてもおおきい | 5 |
| すごくおおきい | 1 |
| 最もおおきい | 0.1 |

price-multは、既存の値（最も〜=4.0、とても〜=2.0）はそのまま残し、新しい
「すごく〜」段階には中間の3.0を割り当てています。

### レア度にSSRを追加、SRの名前を変更
- `SR`の表示名を「すごくめずらしい」→「**とてもめずらしい**」に変更
- 新規`SSR`を追加し、「**すごくめずらしい**」の名前と`light_purple`の色を割り当て
  （キー・名前・色をSRから引き継ぐ形にしています）
- `SR`の色は`yellow`に変更

順序は N（いつもの）＜ R（めずらしい）＜ SR（とてもめずらしい）＜ SSR（すごくめずらしい）
＜ UR（でんせつ）です。weight/price-mult/size-biasは、既存のSR/URの値をベースに、
SSRぶんを間に補って再配分しました（weight: 65/25/7/2/1、price-mult: 1.0/1.8/3.5/5.5/8.0）。

### size-rarity-templates の調整
サイズが9段階になったことで、「ふつう」からの最大距離が3→4に変わったため、
`default`/`gentle`両テンプレートに、新しい距離4（最も〜）ぶんのstepsを追加し、
distance 3（すごく〜）にも自然な値を補いました。

**注意**：`creatures.yml`側で`base-rarity: SR`を指定している生物は、現状ありません
でした（あった場合、名前が自動的に「とてもめずらしい」に変わるだけで、実害は
ありません）。

---

## 34. v0.17.0: custom_model_dataへの切り替え・scale変動幅の抑制

### item_model → custom_model_data に切り替え
`ItemMeta#setItemModel(NamespacedKey)`（新しい item model component 方式）が、
お使いのリソースパックの構造と合わず、黒紫のテクスチャ欠け表示になっていた
とのことでした。より広く使われている **custom_model_data ＋ リソースパック側の
overrides** 方式に切り替えました。

- `Creature`・`GearRegistry.Gear`・`CageService.Cage`の見た目指定フィールドを、
  `NamespacedKey model` → `Integer customModelData` に変更
- `creatures.yml`の`item.model: "summering:xxx"` → `item.custom-model-data: <数値>`
- `config.yml`の`nets`/`rods`/`cages`/`guide`の`model:` → `custom-model-data:`

割り当てた数値（**リソースパック側のoverridesと一致させる必要があります**）：

| 対象 | 範囲 |
|---|---|
| 生物（creatures.yml） | 10001〜10008 |
| 虫取り網 | 20001〜20003 |
| 釣竿 | 20011〜20013 |
| 虫かご | 20021〜20023 |
| 図鑑 | 20031 |

**注意（互換性）**：この変更は破壊的です。リソースパック側も、対応する
material（PAPER等）の基本モデルJSONに、これらの`custom_model_data`値へ
`overrides`で対応させる定義が必要です。

### scaleの変動幅を抑制
`catching.scale`に`clamp-min`/`clamp-max`（既定0.5〜1.6）を追加し、計算結果を
この範囲に丸めるようにしました。調べたところ、処理自体は虫・魚（サーモン系・
トロピカルフィッシュ系問わず）すべて完全に同じ式で、鮭系だから特別扱いしている
箇所はありませんでした。イワナ・ニシキゴイはどちらもsize-min/size-maxの比率が
大きい（3〜4倍）ため、同じ式でも変動幅が大きく出やすい状態でした。今回のclampで
上限・下限は抑えられますが、根本的にさらに幅を抑えたい場合は、該当する生物の
size-min/size-maxの比率自体を狭めるのも効果的です。

### sweepエフェクトについて（対応なし）
`Particle.SWEEP_ATTACK`は色パラメータを持たない固定パーティクルのため、
コード側で色を指定・変更できる箇所がありませんでした。標準の見た目から
変わっていないはずです。もし何か違う色に見えている場合は、詳しい状況を
教えてください。

### 網の射程について（既存機能）
`net-catching.range`は、すでにconfig.ymlで設定可能でした（既存機能）。

---

## 35. v0.17.1: マーカー一覧を右クリックに復帰、一覧画面内での操作を追加

前回、設定棒のスニーク＋右クリックを「新規設置」に割り当てた際、マーカー一覧
（{@link MarkerListGui}）を開く手段がスニーク＋左クリックに移ってしまい、
分かりにくくなっていました。ご要望に合わせて再整理しました。

### 設定棒の操作
- **右クリック**：マーカー一覧画面を開く
- **スニーク＋右クリック**：狙った場所に新規マーカーを設置（変更なし）
- **左クリック**：近くのマーカーを削除（変更なし）
- （スニーク＋左クリックの割り当ては無くなりました。一覧は右クリックに一本化しています）

### マーカー一覧画面内の操作
- **左クリック**：そのマーカーの設定画面を開く
- **右クリック**：そのマーカーの場所へテレポート
- **シフト＋右クリック**：そのマーカーを、今自分がいる場所へ移動（移動後は一覧を再表示します）

これまで「選択→スニーク＋右クリックで移動」という2段階操作だったマーカーの移動が、
一覧画面から直接行えるようになりました。

---

## 36. v0.18.0: 大物ボーナスの再設計（「大きい方が釣れやすい」→「釣れたものが大きくなりやすい」）

### アジ等で「明らかに大きいのにちいさい表示」になる件について
サイズの抽選ロジック（`SizeTier`の重み付き抽選・帯域計算）を一通り見直しましたが、
数値的な矛盾（帯域の順序が逆転している等）は見当たりませんでした。普通の釣竿
（size-bonus: 0.0）では今回の再設計後も何も変化しないため、この現象の説明には
なっていない可能性があります。

考えられる原因として、アジの`size-min`〜`size-max`（10〜35cm）を機械的に9段階へ
割り振っているため、「ちいさい」段階でも実際のcmとしてはそこそこの数値
（帯域の計算上、目安として18cm台〜21cm台）になり得る、というのはあります。
これは内部的には一貫していますが、現実のアジのサイズ感覚とはズレることがあります。
気になる場合は、該当する生物の`size-min`/`size-max`の幅自体を調整すると、
体感に近づけられるかもしれません。それでも明らかにおかしい実例（実際に出た
cmと段階のペア）があれば、教えていただければ改めて調査します。

### 大物ボーナスの再設計
ご要望の通り、`size-bonus`の意味を変更しました。

**旧**：サイズ段階を問答無用で「1段階」ずらす（確率＝絶対値）。
**新**：捕まえたものが「大きくなりやすく」なる。ただし
- 正の値（大物ボーナス）は、ベースのサイズが「ふつう」**以上**のときだけ効果があります
  （「ふつう」未満の個体には効果なし）。小さいものを無理に大きくすると、中心からの
  距離が縮んでレア度が下がってしまうため、あえて対象外にしています。
- 負の値（特殊アイテム用の「小物ボーナス」）は、その逆で「ふつう」**以下**の
  ときだけ効果があります。
- ベースの時点ですでに最高レア度（テーブル最後の項目）に達している個体には、
  大物ボーナスは何もしません（すでに最高レア度のものが、さらに極端なサイズに
  なることはありません）。
- 大物ボーナスが「きっかけ」で最高レア度に到達することはありません（SSRが
  ボーナスの力だけでURになる、ということは起きません）。最高レア度は、
  引き続き“ベースの抽選だけで届いた場合”にのみ到達します。

処理の中身（`SizeTier.Table.upgrade`/`downgrade`）自体は変えておらず、
「いつ・どちらの方向に適用するか」の判定ロジックを追加した形です。

---

## 37. v0.18.1: 見た目と数値の厳密一致オプションを追加

### 「見た目ではAの方が大きいのに、数値ではBの方が大きい」現象について
原因が分かりました。**見た目のscaleは、個体が湧いた瞬間に決まって固定される**のに
対し、**捕まえたときの数値は、その瞬間に使った道具の大物ボーナスによって後から
変わる**ため、同じ「アジ」でも見た目と数値が食い違うことがあります。

具体的には、見た目が小さめの個体でも、良い竿を使えば（大物ボーナスの判定に成功すれば）
数値だけ底上げされます。逆に、見た目が大きめの個体でも、いつもの竿（ボーナス無し）で
釣れば数値は湧いた時のままです。「見た目で比べるとAの方が大きいのに、数値では
Bの方が大きい」というのは、バグというより、この「道具による後乗せ」の仕様が
そのまま現れた結果である可能性が高いです。

個体が湧いた時点では、その後どの道具で捕まえられるか分からないため、完全な
事前一致は原理的に難しい部分です。そこで、`catching.strict-visual-consistency`
（既定`false`）を追加しました。`true`にすると、**個体が実在する捕獲経路
（虫取り網・寄ってくる釣り）では、道具の大物ボーナスを一切効かせなくなります**
（見た目＝数値を厳密に保証）。バニラ釣り（個体が存在しない通常釣り）は、
そもそも見た目が無いため、この設定に関係なく引き続き大物ボーナスが効きます。

既定はOFFのままにしています（良い道具を使うメリットとして、大物ボーナスが実在の
個体にも効く方をこれまで採用してきたため）。見た目と数値の一致を優先したい場合は
`true`にしてお試しください。

---

## 38. v0.18.2: 大物ボーナスを「段階ジャンプ」から「固定倍率」方式に変更

前回の説明の通り、大物ボーナスは「確率でサイズ段階をまるごと1つ引き上げる」という
離散的な処理でした。段階間の実測cmの差は決して小さくないため、これが違和感の
一因になっていた可能性がある、というご指摘を受け、ご提案の通り**固定倍率**方式に
変更しました。

### 新しい処理
実測cmに `(1 + size-bonus)` 倍を直接掛けます（例: size-bonus 0.1 なら常に ×1.1）。
確率要素は無くなり、条件を満たせば常に一定の倍率がかかります。掛けたあとの
サイズ段階・レア度は、その実測cmから逆算して決め直します（生物の
size-min〜size-maxの範囲は超えません）。

適用条件（レア度を下げてしまわないための制約）は前回と同じです：
- 正の値は、ベースのサイズが「ふつう」以上のときだけ効果があります
- 負の値はその逆で「ふつう」以下のときだけ
- ベースの時点で最高レア度に達している場合は適用しません
- 大物ボーナスが「きっかけ」で最高レア度に到達することはありません

### 実装の整理
これに伴い、`applyBonus`（段階決定のみ）と`finalizeSizeCm`（実測cm決定のみ）に
分かれていた処理を、`resolveCatch`という1つのメソッドにまとめました
（倍率計算には「湧いた時点の実測cm」が必要なため、段階とcmを別々に扱う
従来の構造とは相性が悪くなっていました）。虫取り網・寄ってくる釣り・バニラ釣りの
いずれも、最終的にこの1メソッドを通るようになっています。

### 「見た目上サイズを比較するとABが逆転する」件について
大物ボーナスを使わない（size-bonus: 0.0の）網でも起きるとのことでしたので、
大物ボーナスの仕組み自体が直接の原因ではなさそうです。サイズの抽選・帯域計算
ロジックを重ねて確認しましたが、数値的な矛盾は見つけられていません。恐れ入りますが、
具体的にどのペアで（実際に出たcmの数値と段階のラベル、可能であれば使った道具）
逆転が起きたか教えていただけると、次はもっと的確に切り分けられると思います。

---

## 39. v0.18.3: scaleがentityに反映されないことがある不具合を修正（イワナ含む）

添付いただいた画像（cm数値と見た目の大小が逆に見える）を受けて、計算式ではなく
「実際にentityへscaleが適用されているか」を疑って調べ直しました。

原因と思われる箇所を見つけました：見た目のscaleは、`spawnEntity`の生成コールバック
（consumer）の中で設定していましたが、**このタイミングはエンティティが完全に
初期化・ワールドへ追加される前**です。entityの種類によっては、この直後にバニラ側の
初期化処理でAttributeが上書き・リセットされてしまい、コールバック内では正しく
設定できていても、実際には反映されていない（元に戻ってしまう）ことがあるようです。

保険として、**生成が完全に完了した1tick後に、scaleをもう一度掛け直す**処理を
追加しました。これで、コールバック内での設定がリセットされてしまうケースでも、
最終的に正しいscaleが反映されるはずです。

---

## 40. v0.18.4: scale再適用を「1回だけ」から「一定期間しつこく」に強化

前回の1回だけの再適用（1tick後）では、SALMON等、通常の状態でも見た目のサイズが
よく変わりやすい種別に対して間に合わない可能性があるとのご指摘を受け、強化しました。

生成直後から、5tickおきに最大8回（合計で約1.8秒間）、見た目のscaleを繰り返し
掛け直すようにしました。バニラ側の初期化がどのタイミングでAttributeをリセットして
きても、この期間内であれば上書きし返せるはずです。個体が途中で消えた場合は
その時点で再適用を打ち切ります。

---

## 41. v0.18.5: デバッグログの大幅拡充・「最も〜」の実測cm補正・scale二重適用の解消

### デバッグログの拡充
`catching.debug: true`にした際に出るログを大幅に増やしました。
- **素のサイズ段階抽選**（`rollBaseTraits`）：どの段階が選ばれたか、その段階のweightと
  全段階合計のweightを出力（重み付き抽選が実際に比率通り動いているか検証できます）
- **実測cm抽選**（`rollSizeCm`）：どの段階の、何番目/何番目中の帯域から、
  いくつのcmが出たかを出力
- **resolveCatch**（捕まえる瞬間の最終決定）：ベースの段階・cm・レア度、大物ボーナスの
  適用有無、最終的な段階・cm・レア度をひとまとめに出力
- **scale適用**：計算過程に加えて、**setBaseValueで設定した直後に、実際にその値が
  読み戻せるかを検証**し、一致していなければ警告を出すようにしました（同期的に
  何かが上書きしている場合はここで検知できます。1tick以上あとに非同期で上書きされる
  ケースは、前回追加した「生成後しばらく繰り返し再適用」で対応しています）

これらのログを見比べれば、「なぜこの段階が選ばれたか」「なぜこのcmになったか」
「なぜこのscaleになったか」を追えるはずです。「明らかに小さいのに『すごくおおきい』」
のようなケースがまた起きたら、ログと合わせて教えてください。

### 「最も〇〇」の実測cmにも補正をつけるように
ご指摘の通り、これまで「最も〜」段階の実測cmはsize-min/size-maxそのものでしたが、
見た目のscaleだけ別に倍率（既定0.9倍／1.1倍）がかかっていました。ご要望に合わせて、
**実測cm自体に、その倍率を掛ける**ようにしました（`catching.scale.extreme-max/min-multiplier`
をそのまま使い回しています。目安は1.05〜1.2倍／0.80〜0.95倍）。「最もおおきい」は、
定義上のふつうの最大を少し超える、記録級の個体として扱うイメージです。

これに伴い、scale側で別途同じ倍率を掛けていた処理は**廃止**しました（cmに既に
反映されているため、そのままscale計算に使えば同じ結果になります）。以前は、
この2箇所で別々に倍率がかかる＝二重補正になっていたはずで、「すごくおおきい」の
カナブンが7.2cm（size-max: 3.2cmを大きく超える数値）になっていた件も、この
二重適用が絡んでいた可能性があります。

### レア度への影響
レア度の繰り上げは、実測cmではなく**サイズ段階（中心からの距離）**を基準に
決まる仕組みのままのため、今回の変更でレア度側のロジックを変更する必要は
ありませんでした。「最も〜」段階の実測cmが変わっても、その個体が「最も〜」の
段階であること自体は変わらないため、レア度の判定結果はこれまで通りです。

### 「最もおおきいのカナブンが大量発生する」件について
weightの計算式自体は確認済みで、数値上の矛盾は見当たりませんでした（例のweight
だと最もおおきいの出現率は理論上0.1%程度のはずです）。上記のデバッグログで
実際の抽選結果を追跡できるようにしたので、再現した際にログを見ていただけると
原因を切り分けやすくなるはずです。

---

## 42. v0.18.6: 真の原因判明——SALMONのバニラ側サイズ種類（SMALL/MEDIUM/LARGE）を固定

**真の原因が判明しました。** SALMONエンティティには、こちらのscale計算とは全く別に、
バニラ側で湧いた瞬間にランダムに決まる「サイズの種類」（`Salmon.Variant`:
SMALL / MEDIUM / LARGE）があり、これが毎回ランダムに変わっていたことが原因でした。
これまで見た目のscaleを正しく計算・適用できていても、この土台自体の大きさが
バラついていたため、実測cmと見た目が食い違って見えていたわけです。

`Salmon.Variant.SMALL`に固定することで解決しました（イワナ・アジ等、SALMONを
土台にしている生物すべてに効きます）。以降は、こちらのscale計算だけが見た目の
サイズを決めます。

### 整理
ご指摘の通り、前回追加した「生成後しばらくの間、毎tickscaleを掛け直す」処理
（`reapplyScalePersistently`）は不要になったため削除しました。今回の原因は
「scaleの適用が上書きされていた」のではなく「scaleとは無関係な、別のバニラ
プロパティがランダムだった」ことだったため、再適用処理では原理的に直せない
種類の問題でした。`SpawnService`のコンストラクタも、この処理のために追加していた
`Plugin`引数を含めて元の形に戻しています。

前回追加したデバッグログ（素のサイズ段階抽選・実測cm抽選・resolveCatch・scale適用）は
そのまま残しています。今後も動作確認に活用してください。

---

## 43. v0.19.0: SALMONをMEDIUMに変更・トレーダーNPCを追加

### SALMONのサイズ種類をMEDIUMに変更
SMALLだと影が不自然に大きく見えるとのことでしたので、`Salmon.Variant.MEDIUM`に
変更しました。

### 【新規】いきものトレーダーNPC
`/mushitori trader spawn`で、その場に村人ベースのトレーダーNPCを設置できます。

- 右クリックすると、売却確認画面が開きます（「持っているいきもの全部でXXX円」の
  表示＋「確認して売る」ボタン。誤クリック防止のため、確定を押すまでは何も
  売れません）
- 実際の売却処理・金額計算は、既存の`/mushitori sell`コマンドと**完全に同じ**
  ロジック（`DexService#sellInventory`）を使っています
- AIを無効化しており、その場から動かず、戦闘に参加せず、繁殖もしません
- `config.yml`の`trader`セクションで、名前・職業（見た目）を設定できます

### レア度ごとの価格倍率を変更
ご指定の通りに変更しました：

| レア度 | 旧倍率 | 新倍率 |
|---|---|---|
| いつもの (N) | 1.0 | 1.0 |
| めずらしい (R) | 1.8 | 1.5 |
| とてもめずらしい (SR) | 3.5 | 5.0 |
| すごくめずらしい (SSR) | 5.5 | 20.0 |
| でんせつ (UR) | 8.0 | 100.0 |

サイズ段階による価格倍率（`sizes`の`price-mult`）は既存のまま、この2つが
掛け合わされて最終的な売値になります（仕組み自体は元からあったので、
今回は数値の変更のみです）。

---

## 44. v0.19.1: cm表示を小数点3桁に・規格外表示・極端倍率のランダム化

### cmの表示を小数点第3位まで
これまで`%.2f`（小数点2桁）で丸めていましたが、`%.3f`（3桁）に変更しました。
内部の丸め処理自体も3桁精度に変更しています。

### 「規格外に〇〇」表示
`sizes`の各段階に`legendary-name`（任意）を追加しました。通常は`name`が
表示されますが、**その捕獲の最終的なレア度が「でんせつ」（最高レア度）にも
なった場合だけ**、`legendary-name`が設定されていればそちらを表示します。
既定で「最もちいさい」→「規格外にちいさい」、「最もおおきい」→「規格外に
おおきい」を設定しています（この2箇所以外は特に必要ないはずなので、
legendary-nameは省略したままにしています）。

### 「最も〇〇」の倍率をランダム化
`catching.scale`の`extreme-max-multiplier`/`extreme-min-multiplier`（固定値）を、
`extreme-max-multiplier-min`/`-max`、`extreme-min-multiplier-min`/`-max`
（範囲指定）に変更しました。既定はそれぞれ1.05〜1.2倍／0.80〜0.95倍で、
「最も〜」段階の実測cm・見た目のscaleを決めるたびに、その都度この範囲から
ランダムに1つ選びます。

### 道具の効果で上限（下限）に張り付く場合の乱数
「ふつう」等、極端でない段階から出発した個体が、道具の大物ボーナスによって
size-max相当（または下限）まで押し上げられる場合、これまでは常に同じ
数値（上限ちょうど）になっていました。ご要望に合わせて、この場合も
±5%（0.95〜1.05倍）のランダムな揺らぎを加えるようにしました。

---

## 45. v0.20.0: 網・釣竿のステータスをアイテム自身に焼き付け・網の射程を道具ごとに

### min-sizeの補正について（確認）
ご質問いただいた「最もちいさい」側の補正・乱数は、v0.19.1の時点で既に「最もおおきい」
側と同じ扱いで実装済みでした（`rollExtremeMinMultiplier`、クランプ時のjitterともに
両方向に対応しています）。

### 網・釣竿のステータスをアイテムに焼き付ける方式に変更
ご要望の通り、config.ymlは「/mushitori give で何を渡すかのテンプレート」という
位置づけに変更しました。実際に渡した**瞬間**の値（捕まえやすさ補正・大物ボーナス・
射程）が、その場でアイテム自身に焼き付けられます。以降、そのアイテムを使うときの
効果は、config.ymlの現在値ではなく、アイテムに焼き付けられた値をそのまま参照します。

- 例：`size-bonus: 3`のときに`/mushitori give net my_net`で受け取った網は、
  以後ずっと大物ボーナス3倍のまま使えます。あとで config.yml の `size-bonus` を
  `-3` に変えても、**既に配布済みのその網は影響を受けません**。次に
  `/mushitori give` したものから、新しい`-3`の効果が適用されます。
- 耐久値は、もともとバニラのDamageableコンポーネント自体がアイテムに焼き付く
  仕組みだったため、同じ考え方が自然に成り立っていました（`GearRegistry.damage`が
  config参照だった点だけ、今回あわせて修正しています）。
- IDをconfig.ymlに追加するだけで、自由に新しい網・釣竿を増やせる点は変わりません
  （むしろ、この方式のほうが「過去に配った分は変えずに、新しい網だけ追加・調整する」
  という運用がしやすいはずです）。

### 網の射程を道具ごとに設定可能に
`nets`の各IDに、任意で`range`を指定できるようにしました（省略時は
`net-catching.range`の全体既定値を使います）。射程も他のステータスと同様、
渡した瞬間にアイテムへ焼き付けられます。

---

## 46. v0.20.1: マーカー湧きの地面判定・寄ってくる釣りの多人数時の取り合い対策

### 虫が地面に埋まる（宙に浮いた画像の件）
マーカーの湧く位置（`spawn-radius`内のランダムな地点）が、これまでX/Zだけを
ランダムに決め、Yはマーカーの高さをそのまま使っていました。崖際・浮島の端など、
その範囲内に地面が無い場所へたまたまランダムで選ばれると、空中や void の上に
湧いてしまい、重力で落ち続けた結果、地形の下に埋まったように見えていたのだと
思われます。

X/Zをランダムに決めたあと、その付近を上下に探索し、**実際に乗れる地面・浸れる
水面を見つけてから、その真上に湧かせる**ように修正しました。何度か試しても
見つからない場合は、マーカーの中心そのもの（最初に設置した場所）を最後の
保険として使います。

### 寄ってくる釣りの、複数プレイヤーによる取り合い対策
すでに「他のプレイヤーと同時に取り合われないようにする」仕組み
（`reservedFish`）自体はありましたが、**「反応判定をしたタイミング」と
「実際に確保するタイミング」の間にわずかな隙**があり、複数のプレイヤーが
ほぼ同時に同じ個体へ反応判定していた場合、理論上どちらも「まだ確保されて
いない」状態を見てしまう可能性がありました（特にFoliaのような、複数の
スレッドが同時に動く環境で起こりえます）。

個体を選んだ「その場で」確保するように変更し、この隙を無くしました
（`Set#add`の戻り値を使って、確保できたかどうかを1回の操作で確実に判定
しています）。取り合いに負けた側は、静かに次の判定タイミングまで
もう一度待つ形になります。

---

## 47. v0.20.2: 「最も〜」の補正倍率が固定値に近くなる抜け道を修正

`rollExtremeMaxMultiplier`/`rollExtremeMinMultiplier`（「最も〜」段階の実測cmに
掛ける倍率）を確認したところ、乱数の生成ロジック自体（`ThreadLocalRandom`）は
元々十分な精度（小数点以下多数桁）を持っていましたが、**config側でmin/maxの幅が
0またはごく小さい場合、乱数を使わずmin側の固定値をそのまま返す**分岐が
残っていました。ここが「1倍に近い、代わり映えのしない結果が多く見られる」の
原因になっていた可能性があります。

この分岐を撤去し、**min/maxがどんな値でも、常に最低限の幅（±1%程度）を
確保したうえで、必ずランダムに1つ選ぶ**方式に変更しました。これにより、
`config.yml`側の設定に関わらず、常に細かい小数点まで変動する倍率になります。

---

## 48. v0.21.0: トレーダーを個別売却対応に・サイズによる連続的な価格補正

### トレーダー画面で個別に売れるように
これまでは「全部売る」の確認画面だけでしたが、手持ちの売れるいきものアイテムを
1つずつ並べて表示し、**クリックでそのアイテムだけ**売れるようにしました。
「全部売る」ボタンも引き続きあります。

### トレーダー画面でだけ売値を表示
アイテムの説明文に「売値 ○○円」の行を追加していますが、これは**トレーダー画面上で
見せているコピー（プレビュー）にだけ**追加しており、実際のアイテム自体の説明文は
変更していません。画面を閉じれば、通常通りの表示に戻ります。

### サイズによる連続的な価格補正
これまで売値は「レア度のprice-mult × サイズ段階のprice-mult」で決まる、段階ごとの
段差のある計算でした。ご要望に合わせて、**同じサイズ段階の中でも、実測cmが
「ふつう」（中心）から離れているほど高く売れる**、なめらかな補正を追加しました。
例えば同じ「おおきい」でも、その段階の中でより大きい個体のほうが高く、同じ
「ちいさい」でも、その段階の中でより小さい個体のほうが高くなります（レア度は
問いません。「いつもの」でも他のレア度でも同じように働きます）。

`catching.size-continuous-price-bonus`（既定0.5）で強さを調整できます。0.5だと、
段階内で最も極端な個体（size-min/size-maxちょうど）は、中心寄りの個体のおよそ
1.5倍で売れます。0にすると、この補正自体を無効にできます（従来通りの、段階だけの
価格に戻ります）。

---

## 49. v0.21.1: 大物ボーナスを「倍率」から「幅に比例した加算」方式に変更

ご指摘・ご提案いただいた通りに修正しました。

**旧**：実測cmに `(1 + size-bonus)` 倍を掛ける。size-min/size-maxの幅が狭い生物
（蝶等）ほど、同じsize-bonus値でもサイズ段階が一気に何段も飛びやすく、レア度が
急変しやすい問題がありました。

**新**：`(size-max - size-min) × size-bonus ÷ 2` を実測cmに**加算**する。
生物ごとの幅に比例するため、幅が狭い生物でも広い生物でも、同じsize-bonus値なら
常に「同じ割合」だけ動きます。÷2は、ご指摘の通り「size-bonus=1.0のとき、ちょうど
平均サイズの個体がsize-max（またはsize-min）ちょうどに届く」ように調整するための
ものです（検証済み：蝶で計算すると 9.25 + 2.75 = 12.0 ＝ size-maxと一致）。

道具の効果で上限・下限に張り付く場合の処理（±5%の乱数）や、大物ボーナスの
適用条件（ベースが「ふつう」以上／以下のときだけ効く、最高レア度への到達を
きっかけにしない等）は変更していません。

**注意**：既存の`size-bonus`の数値（0.05、0.12等）は、旧方式（倍率）を前提に
仮で置いていた値です。新方式では同じ数値でも変化の絶対量が変わります
（多くの場合、より穏やかになります）。実際の挙動を見ながら、必要であれば
数値を調整してください。

---

## 50. v0.21.2: 大物ボーナスの「ふつう以上／以下限定」の制限を撤廃

前回、大物ボーナスを「幅に比例した加算」方式に変更したことで変動量が予測しやすく
なったため、ご提案の通り、「ベースが既にふつう以上（大物ボーナスの場合）／
以下（小物ボーナスの場合）のときだけ効く」という制限を撤廃しました。

これにより、大物ボーナス付きの網で「ちいさい」個体を捕まえても、常に大きくなる
方向へ加算されます（小物ボーナスならその逆）。ベースのサイズ次第では、結果的に
中心（ふつう）へ寄ってレア度が下がる場合もあり得ますが、ご指示通りその挙動を
許容する形にしています。

ご指定の通り、**でんせつサイズ（素の時点で既に最高レア度＝最もちいさい／
最もおおきいだったもの）には、引き続き適用しません**（この部分の挙動は変更して
いません）。

---

## 51. v0.21.3: 所持金の仕組みを紙幣・硬貨方式に作り直し（エラー修正）

### 原因
ご指摘の通りでした。所持金は「コイン」アイテム1個のスタック数で表現していましたが、
大きな金額を扱うために`ItemMeta#setMaxStackSize(Integer.MAX_VALUE)`としていた
箇所が、Paperの実際の制約（**1〜99の範囲でなければならない**）を超えており、
一定額以上を渡そうとするとエラーで落ちていました。

### 修正内容
ご提案の通り、紙幣・硬貨の組み合わせ方式に作り直しました。既定の額面は
1万円札・5000円札・1000円札・500円玉・100円玉・50円玉・10円玉・5円玉・1円玉の
9種類です。

所持金を渡すときは、額面の大きいものから順に、できるだけ少ない枚数になるよう
自動で組み合わせます（貪欲法）。例：1211円 → 1000円札1枚、100円玉2枚、
10円玉1枚、1円玉1枚。1円玉（額面1）を含めているため、どんな金額でも端数なく
ちょうど渡せます。

`config.yml`の`money`セクションで、額面ごとの名前・素材・custom-model-dataを
変更できます（`denominations`）。

### 互換性について
- 所持金を数える`money.get()`・渡す`money.add()`/`set()`等の外部から見た
  使い方（メソッドの呼び出し方）は変わっていません。
- **旧方式の「所持金コイン」アイテム（エメラルド）を既に持っている場合、
  そのアイテムは新しい仕組みでは所持金として認識されません**（データ形式が
  変わったためです）。お手数ですが、該当のプレイヤーがいる場合は
  `/mushitori money set <プレイヤー> <金額>`で所持金を設定し直してください。

---

## 52. v0.21.4: 「最もおおきい」のサイズによる価格差が付かない不具合を修正

### 原因
`price()`のサイズ連続補正で使っていた`Creature#sizeRatio()`が、0〜1の範囲に
**クランプ**する仕様でした。「最も〜」段階は、実測cmがsize-min/size-maxを
1.05〜1.2倍（既定）ほど超えることがある仕様のため、「最もおおきい」の個体は
**どんな実測cmでも比率が必ず1.0に丸められてしまい**、結果として細かい大小差が
価格に反映されず、同じ価格になっていました。

### 修正内容
価格計算専用に、0〜1へクランプしない比率の求め方に変更しました。これにより、
「最もおおきい」同士でも、実測cmが大きいほど（＝掛かった倍率が1.2倍に近いほど）
きちんと高い価格になります。「最もちいさい」側も同様です。非極端な段階
（ちいさい〜おおきい）の価格計算には影響ありません（実測cmがもともと
size-min〜size-maxの範囲に収まるため、クランプの有無で結果が変わらないためです）。

---

## 53. v0.21.5: 上限張り付き時に同じサイズばかり出る不具合を修正

いただいたデバッグログ（`cm=14.4`が複数回連続）から原因が分かりました。道具の
効果で上限に張り付く場合の乱数を「上限 × (0.95〜1.05)」で決めていましたが、
乱数が1.0を超えた分は、直後の「範囲内に丸める」処理で**強制的に上限ちょうど**へ
引き戻されていました。乱数の範囲が上限をまたいでいたため、上振れした結果が
すべて同じ値（上限ちょうど）に潰れてしまい、かえって同じサイズが頻発する
原因になっていました。

乱数の範囲を「上限を超えない側だけ」（上限の95%〜100%）に変更し、下限側も
同様に「下限を下回らない側だけ」（下限の100%〜105%）に変更しました。これで
後段のクランプ処理が事実上何もしなくなり（範囲を最初からはみ出さないため）、
乱数の結果がそのまま活きるようになります。

---

## 54. v0.21.6: マーカー関連のまとめ修正（削除方法変更・キー入れ替え・同時湧き最大数・戻り位置）

### ①設定棒：削除は設定画面からのみに
左クリックが「近くのマーカーを削除」だったのを、**視線の先（カーソルが合っている）
マーカーの設定画面を開く**動作に変更しました。削除は、設定画面内の「削除」ボタンから
のみ行えます（誤操作防止）。マーカーを狙って判定するための
`AmbientSpawnService#markerAlongRay`を新設しました（視線からの外れ具合が一番
小さいものを選びます）。

### ②生物枠のウェイト操作キーを入れ替え
ご指定の通りに変更しました。

| 操作 | 旧 | 新 |
|---|---|---|
| 左クリック | 別の生物に切り替え | 変更なし |
| シフト＋左クリック | ウェイト +1 | 枠を空にする |
| 右クリック | ウェイト -1 | 変更なし |
| シフト＋右クリック | 枠を空にする | ウェイト +1 |

### ③近くにいると追加で湧かなくなる件
明確な原因は特定できませんでしたが、`tick`の巡回状況（生存数・直近捕獲数・
枠の空き状況）をデバッグログに出すようにしました。あわせて④の「同時湧き最大数」を
実装したことで、それまで「1巡回につき1匹しか増えない」実装だったのが、
複数の枠をまとめて埋められるようになったため、体感的な「湧きが遅い・
止まっているように見える」点は緩和されるはずです。再現した場合は、
デバッグログと合わせて教えてください。

### ④同時湧き最大数を追加
`simultaneous-max`（既定1）を追加しました。1巡回で、残り枠と
simultaneous-maxの小さい方の数だけ「候補枠」を用意し、**それぞれ独立に
spawn-chanceを判定**します。

- 最大数3・生存1・simultaneous-max2・spawn-chance100% → 残り枠2、候補2枠とも
  成功 → 確実に2匹湧く
- 最大数3・生存2・simultaneous-max2・spawn-chance100% → 残り枠1（simultaneous-maxの
  2より少ない）、候補1枠のみ、成功 → 確実に1匹湧く
- 最大数3・生存0・simultaneous-max2・spawn-chance50% → 候補2枠、それぞれ独立に
  50%判定 → 0〜2匹のどれかが湧く

設定画面にもスロットを追加しています。

### ⑤範囲外から戻るときの位置
これまでも中心ではなく範囲内のランダムな地点へ戻していましたが、高さ（Y座標）は
個体の現在の高さをそのまま使っており、地面・水面を考慮していませんでした。
新規湧き時に使っている「地面・水面を探して安全な高さを決める」処理
（`findSafeSpawnPoint`）を、戻るときにも使うようにしました。見つからない場合は、
保険として従来通りの動きにフォールバックします。

---

## 55. v0.21.7: 道具ボーナスによる最大サイズと、でんせつ級サイズの範囲を分離

ご指摘・ご提案の通りでした。道具の大物ボーナスで上限に張り付く場合の基準を、
`size-max × extreme-max-multiplier`（例：12.0×1.2＝14.4）にしてしまっていたため、
道具の力だけで到達できるサイズと、素の抽選だけで届く「最もおおきい」（でんせつ級）の
サイズ範囲が広く重なってしまっていました。

`size-max`（12.0）そのものを基準にし、乱数範囲も**1.0〜1.05倍**に変更しました。

- 道具ボーナスで上限に張り付いた場合：size-max × 1.0〜1.05倍（蝶なら12.0〜12.6cm）
- 「最もおおきい」（素の抽選だけで届いた、でんせつ級）：size-max × 1.05〜1.2倍
  （蝶なら12.6〜14.4cm）

ちょうど12.6cmで両者が接する形になり、ご指摘いただいた「段階の混在」
（例：11.9cmが『最もおおきい』と『すごくおおきい』のどちらにもなり得る）も、
常にsize-max以上の値になるよう下限を1.0倍にしたことで解消されます
（size-maxちょうど以上なら、必ず「最もおおきい」段階として扱われます）。
下限側（size-min未満になる場合）も同様に対称に修正しています。

---

## 56. v0.22.0: トレーダー画面を「渡すもの」エリア方式に全面刷新

ご要望に合わせて、トレーダー画面を作り直しました。

### 「渡すもの」エリア
9列×4段（36マス）のうち、中央のまとまった枠（14マス）が「渡すもの」エリアです。
自分の持ち物から、売りたいいきものアイテムをドラッグ＆ドロップでここへ入れると、
それが売却対象になります。「確定」ボタンを押すまでは、何も売れません。それ以外の
枠は板ガラスで塞いであり、アイテムを置けないことがひと目で分かるようにしています。
（万一何かの拍子に売れないものが紛れ込んでも、1tick後に検証して自動的に持ち物へ
押し戻すようにしています。）

「全部売る」ボタンも残しており、こちらは従来通り「渡すもの」を使わず持ち物全体から
まとめて売れます。

### 画面を開いている間の売値表示
トレーダー画面を開いている間だけ、自分の持ち物（下段）にあるいきものアイテムにも
「売値」の行が一時的に追加されます。画面を閉じると元の表示に戻ります（実際の
アイテムには何も残りません）。「渡すもの」に置いたまま画面を閉じた場合も、
売値の表示を剥がしてから持ち物へ返します。

### 図鑑未登録の確認
「確定」または「全部売る」を押した際、対象に図鑑未登録のいきものが含まれている
場合は、即座には売らず「本当に売りますか？（未登録あり）」という警告表示に
切り替わります。もう一度同じボタンを押すと、そのまま売れます。

---

## 57. v0.23.0: 図鑑の登録画面を「登録するもの」エリア方式に刷新（トレードと同じ考え方）

### 図鑑の登録もトレードと同じ「渡すもの」方式に
図鑑アイテムの**右クリックでインベントリ全部を即登録する機能は廃止**しました。
代わりに、新しい登録画面（{@code DexRegisterGui}）を開くようにしています。

- 9列×4段のうち中央14マスが「登録するもの」エリア。持ち物から登録したい
  いきものをドラッグして入れ、「確定」ボタンで登録します
- 「持ち物を全部登録」ボタンで、従来通り持ち物全体から未登録のものをまとめて
  登録することもできます（UIの中に、個別登録とまとめて登録の両方を用意しています）
- 登録は売却と違ってアイテムを消費しないため、「登録するもの」に置いたまま画面を
  閉じても、そのまま持ち物へ返されるだけです（何も失われません）

図鑑アイテムの右クリックは「登録画面を開く」に、スニーク＋右クリックは
これまで通り「図鑑を開く（閲覧）」のままです。図鑑メイン画面の「登録」ボタンも、
即登録ではなく登録画面を開くように変更しています。

### 自分で捕まえていないいきものを登録する際の確認
「確定」「持ち物を全部登録」いずれも、対象に**自分以外が捕まえたいきもの**
（アイテムの捕獲者情報が自分と一致しないもの）が含まれている場合は、即座には
登録せず「本当に登録しますか？」という警告表示に切り替わります。もう一度同じ
ボタンを押すと、そのまま登録されます（トレードの「図鑑未登録の確認」と同じ
2段階確認の考え方です）。

なお、`register.allow-others-catch`をfalseにしている場合は、これまで通り
他人の捕獲は最初から登録できません（確認なしで弾かれます）。この確認は、
allow-others-catchがtrue（既定）のときに働きます。

---

## 58. v0.24.0: 生物ごとの「独特の動き」設定を新設（蝶の蜜集め対策含む）

### 蝶の蜜集めを無効化
蝶（fly/yellow_fly、entity: BEE）が花の蜜を集めると、バニラ側の仕様で見た目が
変わったり花粉のパーティクルが出たりする件、原因が分かりました。BEEには
「花を探す→蜜を集める→巣（無ければ作物）へ運ぶ」という一連のバニラAIがあり、
これが動いている限り、`setHasNectar(false)`のような一時的な対処だけでは
すぐにまた蜜を集めに行ってしまいます。

Paperの Mob Goals API（`Bukkit.getMobGoals()`）を使い、該当のバニラAIそのものを
個体ごとに取り除くようにしました（`BEE_LOCATE_HIVE` / `BEE_GO_TO_KNOWN_FLOWER` /
`BEE_POLLINATE` / `BEE_GROW_CROP` / `BEE_GO_TO_HIVE` / `BEE_ENTER_HIVE`）。

### 【新規】生物ごとの「独特の動き」設定（behavior）
ご要望の通り、今後も増えていくであろう「生物ごとの独特の動き」のための仕組みを
作りました。creatures.yml の各生物に、任意で`behavior`セクションを追加できます。

| 設定項目 | 内容 | 既定値 |
|---|---|---|
| `disable-nectar` | BEEの蜜集めAIを無効化するか | true |
| `flee-from-players` | 近くのプレイヤーから物理的に走って逃げるか | false |
| `flee-radius` | 逃げ始める距離（ブロック） | 5.0 |
| `flee-speed` | 逃げるときの移動速度倍率 | 1.0 |
| `movement-speed-multiplier` | 通常の移動速度倍率（1.0未満でゆっくり） | 1.0 |
| `escape-despawns` | 逃げられたとき、近くへ瞬間移動せず完全に消えるか | false |

- **近づくと逃げる**：`flee-from-players: true`。新規に自作のPaper Mob Goal
  （`FleeFromPlayersGoal`）を追加する方式です。瞬間移動や消滅ではなく、
  パスファインダーで実際にプレイヤーの反対方向へ走って離れていきます。
- **ジャンプして移動する**：うさぎ・スライムのような跳ねる動きは、その
  entity自体が元々持っている挙動のため、新しい設定は不要です。
  `entity: RABBIT`や`entity: SLIME`等を指定するだけで実現できます。
- **ゆっくり動く**：`movement-speed-multiplier`で調整できます（Attribute.MOVEMENT_SPEEDに
  掛け合わせます）。
- **逃げると完全に消える**：`escape-despawns: true`。虫取り網・寄ってくる釣り
  どちらの「捕まえそこねて逃げられた」場面でも、近くへの瞬間移動ではなく、
  その場で完全に消えるようになります（マーカーから新しく湧くのを待つ形になります）。

---

## 59. v0.25.0: 道具タグ・釣竿の耐久・網の捕獲数ベース耐久・nets/rods.yml分離・逃走音

ご依頼いただいた5点、まとめて実装しました。

### ①道具のタグ制度
`creatures.yml`の各生物に`required-tags`（任意）、`nets.yml`/`rods.yml`の各道具に
`tags`（任意）を追加できるようにしました。生物にrequired-tagsが設定されている場合、
道具側のタグがそれを全て含んでいないと、その生物はそもそも捕獲の対象になりません
（捕まえられることも、逃げられることもなく、完全に素通りします）。虫取り網・
通常釣り・寄ってくる釣り、すべてに対応しています。タグも他のステータスと同じく
道具アイテム自体に焼き付けられます。

### ②釣竿の耐久値
`rods.yml`に`durability`を追加しました（既定値も設定：いつもの竿40、ふつうの
竿90、名人の竿は無制限）。通常釣り・寄ってくる釣り、どちらで使っても、実際に
釣れたときに耐久が減ります。ご指定の通り、**寄ってくる釣りは、確率判定で失敗して
逃げられたときも耐久が減ります**（通常釣りは、アタリが来てから逃げられた場合も
同様に減ります）。

### ③虫取り網の耐久を「捕まえた回数」ベースに
これまでは「振った回数」ごとに一律1減っていましたが、ご指定の通り、実際に
「捕まえた（＋逃げられた）」回数ぶん減るように変更しました。1回の振りで複数匹
巻き込んだ場合はその数ぶん、何も対象がいなかった振りでは全く減りません。

### ④nets.yml / rods.yml への分離
釣竿・虫取り網の設定を、config.ymlから独立した専用ファイル（`nets.yml` /
`rods.yml`）に分離しました。creatures.ymlと同じ考え方で、サーバーの
プラグインフォルダに自動生成されます。config.ymlには、道具そのものの定義ではなく
「虫取り網・釣りの挙動そのもの」に関する設定（net-catching・fishing等）だけが
残っています。

### ⑤逃走時の効果音
テレポート音（ENTITY_ENDERMAN_TELEPORT）から変更しました。
- 虫取り網（net-catching.sound-escape）：既定 `ENTITY_PLAYER_ATTACK_NODAMAGE`
  （「スカッ」に近い、空振り寄りの軽い音）
- 釣り（fishing.sound-escape、通常釣り・寄ってくる釣り共通）：既定
  `ENTITY_GENERIC_SPLASH`（「ジュポン」に近い、水の音）

**1点確認させてください**：いただいた指示に「虫が逃げる音を…」という行が2つあり、
片方が「スカッ」、もう片方が「ジュポンッ（水の音）」というご指定でした。水の音は
魚の逃走時を想定されているのではと考え、上記のように**虫＝スカッ系、魚＝水音系**で
実装しています。もし意図と違う場合は教えてください（config.ymlの2箇所の設定値を
入れ替えるだけで対応できます）。

---

## 60. v0.25.1: 実績データパックの自動再読み込み処理を削除

`AdvancementService`にあった、進捗データパックを認識させるための自動処理
（起動時に`minecraft:reload`でサーバー全体のデータを再読み込みし、続けて
`/datapack enable`する、という2段階のコマンド自動実行）を削除しました。

データパックのファイルをコピーする処理（`install()`）自体は残しています
（`datapacks/<パック名>/`にファイルを書き出すだけの、軽い処理のため）。
有効化（`minecraft:reload` → `/datapack enable "file/<パック名>"`）は、
コンソールから手動で実行してください。一度有効化すれば、そのワールドでは
以後も有効なままなので、初回だけのひと手間です。ログにも、まだ有効化されて
いない場合にこの2コマンドを案内するメッセージを出すようにしています。

未使用になった`installed`フィールドも合わせて削除しました。

---

## 61. v0.26.0: 【新規】いきものを「自慢」する機能

ご要望の「自慢」機能を実装しました（`ShowcaseListener`）。

### 操作
スニーク（Shift）を`showcase.hold-seconds`（既定1.5秒）以上押し続けたまま、
アイテムを捨てる操作（Q）をすると、実際には手放さずに、目の前
（`showcase.distance`、既定2.2ブロック先）にそのいきものが実体として現れます。
名前の上に「★ レア度 生物名 (サイズ段階名 実測cm)」を表示します（レア度の色付き。
でんせつサイズの場合は「規格外に〜」の表示にもきちんと対応しています）。

### 見た目・サイズ
`CatchService#applyScale`（実際に捕まえたときと全く同じ計算式）をそのまま使い、
捕まえた瞬間の実測cm・サイズ段階に応じたscaleを再現しています。でんせつなどの
特別なサイズ補正も、そのまま引き継がれます。

### 安全策
- 虫取り網の捕獲対象判定は`creature_id`タグの有無で行っていますが、この
  表示用の個体には最初からこのタグを付けていないため、そもそも捕獲・逃走判定の
  対象になりません
- 念のため、無敵化（`setInvulnerable`）・AI無効化（NoAI）・非衝突化も設定しています
- `EntityDamageEvent`側でも、表示用の個体（専用タグで判別）へのダメージは
  重ねて無効化しています

### 解除条件
- 実際に移動すると（`showcase.move-tolerance`、既定0.35ブロック以上。見回すだけでは
  消えません）自動的に消えます
- 誰も動かなくても、`showcase.max-duration-seconds`（既定30秒）経てば自動的に消えます
- ログアウト時にも片付けます

---

## 62. v0.26.1: 【新規】アイテムを直接渡す機能

ご要望の「アイテムを渡す」機能を実装しました（`PlayerGiveListener`）。

### 操作
スニーク（Shift）＋右クリックでプレイヤーに使用すると、相手のメインハンドが
空いている場合、持っているアイテムをそのまま手渡せます。**バニラのアイテムも
含めて、どんなアイテムでも対象**です（いきものアイテムに限りません）。

### 失敗時
相手が既に何か持っている場合は渡せません。ご指定の通り、「ポロン」に近い軽い
効果音（既定 `BLOCK_NOTE_BLOCK_PLING`）を鳴らします。成功時の効果音・失敗時の
効果音は、どちらも`config.yml`の`player-give`セクションで変更できます。

### 「自慢」機能との優先順位
ご指摘の通り、「自慢」機能もスニークがきっかけになるため、自慢の構え中
（スニーク保持中）・表示中は、こちらの「アイテムを渡す」判定を一切行わないように
しました（`ShowcaseListener#isChargingOrShowing`で確認しています）。自慢が
優先され、渡す機能は何も起きません。

---

## 63. v0.27.0: 【新規】素手での捕獲・魚は釣竿限定に

### 素手での捕獲
メインハンドに何も持っていない状態でいきものを右クリックすると、捕まえられる
ようになりました（`BareHandCatchListener`）。

確率は以下の通り計算します：
1. 道具無しでの捕まえやすさ（`1 - creature.escape-chance`）を求める
2. そこから一律 **-50%**（本来100%で捕まえられる生物でも、素手では50%まで下がる）
3. 対象が空を飛ぶ生物（`creature.flying`。省略時はentityがBEEならtrue）の場合は、
   そこからさらに **×0.1**（例：50% → 5%。9割方失敗するようになる）
4. 対象が魚（FISHカテゴリ）の場合は、そもそも対象外（次項参照）

`required-tags`が設定されている生物は、素手（タグ無し）では最初から対象外です。
道具のボーナスは働かないため、大物ボーナスは常に0（生物本来のサイズ・レア度の
まま）です。

### 魚は釣竿限定に
ご指定の通り、魚（FISHカテゴリ）は**網でも素手でも捕まえられなく**しました。
これまでは、カテゴリは図鑑・実績の整理用という位置づけで、網でも魚を
捕まえられてしまっていましたが、今回明確に「魚は釣竿限定」としました。

---

## 64. v0.28.0: 【新規】貯金箱機能

ご要望の「貯金箱」機能を実装しました。

### 導線（仮）
図鑑メイン画面の「所持金」表示（スロット16）をクリックすると、貯金箱画面が
開きます。ご指定の通り、ひとまずこの導線にしています（あとで変える前提で、
`SavingsGui.open()`を呼ぶだけの1行で、他の場所からも簡単に開けるようにして
あります）。

### 仕組み
所持金とは別に、スコアボード（`dummy`のObjective、プレイヤー名をエントリーに
使用）で貯金額を管理する`SavingsService`を新設しました。サイドバー等の表示
スロットには固定していません（他のプラグイン・サーバーの見た目を邪魔しない
よう、あくまで内部管理用のObjectiveとして扱っています。`/scoreboard players
list`等のバニラコマンドからも確認できます）。

### 画面の内容
仮の実装として、まずは「全部預ける」「全部引き出す」の2択にしています。

- **全部預ける**：今持っているお金（紙幣・硬貨）を全て貯金に回します
- **全部引き出す**：貯金額ぶんの紙幣・硬貨を、これまで通りの額面の組み合わせ
  （既存の`MoneyService`のロジックをそのまま使用）で持ち物へ渡します

---

## 65. v0.29.0: 引き出し金額の選択・小銭入れ/財布の追加

### ①貯金箱：引き出す金額を選べるように
貯金箱画面に、引き出す金額を自由に決めるための段階ボタン（±1／±10／±100／
±1000／±10000。左クリックで＋、右クリックで－）を追加しました。決めた金額は
「この金額を引き出す」で確定します。「全部引き出す」もそのまま残しています。
「引き出す金額をリセット」「貯金額いっぱいに」のショートカットも用意しました。

### ②【新規】小銭入れ・財布
虫かごと同じ考え方の、お金専用の入れ物を追加しました。

- **小銭入れ**（`/mushitori give purse coin_purse`）：容量5。硬貨（小銭）のみ
- **財布**（`/mushitori give purse wallet`）：容量9。紙幣・硬貨どちらのお金も

判定のため、`MoneyService`の各額面に「紙幣か硬貨か」の区別（`Type`）を追加し、
アイテムにも焼き付けるようにしました（`config.yml`の`money.denominations`で
`type: BILL`または`type: COIN`を指定できます。省略時は額面1000以上を紙幣、
未満を硬貨とみなします）。

虫かご以外のいきものを入れられないのと同様に、小銭入れには紙幣を、財布には
お金以外のものを入れることはできません（1tick後に中身を検証し、条件を
満たさないものは自動的に持ち物へ押し戻します）。

---

## 66. v0.30.0: 図鑑の右クリック動線変更・トレード/登録画面を9x6・36枠に拡張

### ①図鑑アイテムの右クリック動線を変更
右クリックで直接登録画面へ行く仕様をやめ、右クリックで図鑑（閲覧）画面を開き、
そこから「図鑑に登録する」ボタンで登録画面へ進む形に変更しました。スニーク＋
右クリックの特別な割り当ては無くなりました（右クリック1つに一本化しています）。

### ②③トレード・登録画面を9列×6段に拡張、「全て選択／解除」方式に変更
トレード画面（`TraderGui`）・図鑑登録画面（`DexRegisterGui`）を、どちらも
9列×4段（36マス）から**9列×6段（54マス）**に拡張し、「渡すもの／登録するもの」
エリアも7列×2段（14マス）から**9列×4段（最大36マス）**に拡大しました。

「持ち物を全部売る／全部登録」ボタン（押した瞬間に一括処理していたもの）は
廃止し、**「全て選択」**ボタンに置き換えました。

- 「全て選択」：持ち物にある売れる（登録できる）いきものを、空いている枠へ
  まとめて移動します（最大36枠ぶん。入りきらない分は持ち物に残ります）
- もう一度押すと **「全て解除」** に変わり、枠の中身を持ち物へ戻します
- どちらの場合も、実際に売る・登録するには、引き続き「確定」ボタンを押す
  必要があります（選択しただけでは何も処理されません）

### エラー対策
ご指摘いただいた「解除の際に持ち物が埋まっていて戻せない場合」は、既存の
仕組みがそのまま対応していました：「全て解除」で持ち物へ戻しきれなかった分は、
その枠にそのまま残ります。その状態で画面を閉じても、（元々の実装通り）
持ち物に入る分は持ち物へ、入りきらない分は足元にドロップされます。

---

## 67. v0.31.0: 自慢機能を大幅に調整（距離・配置・キー・消える条件・表示レイアウト）

ご要望を全て反映しました。

### ①距離を近く
既定を2.2ブロック→**1.5ブロック**に変更しました。

### ②配置方法の改善
正面に地面・壁がある場合は、その手前（視線＋pitch込みの本当の向きで判定）に、
何も無い（空中を見ている）場合は、体の向き（yaw）だけを使った水平方向・
指定距離の位置に表示するようにしました。真上・真下を向いていても破綻しません。

### ③トリガーをドロップから「シフト＋右クリック」に変更
誤って捨ててしまう事故を防ぐため、構え成立後のトリガーを、アイテムを
捨てる動作（Q）から**右クリック**に変更しました。

### ④消える条件を「表示した個体からの距離」に変更
これまでは「設置した瞬間のプレイヤーの位置から動いたか」で判定していましたが、
ご指摘の通り、**表示した個体そのものからの距離**（既定2ブロック）で判定する
ように変更しました。

### ⑤向きを「相手側」に変更
表示したいきものの向きを、プレイヤーの方（自分側）を向く形から、**プレイヤーが
見ていたのと同じ方向**（＝プレイヤーに背を向ける形）に変更しました。

### ⑥表示レイアウトを段組みに
バニラのカスタムネームは1行しか表示できないため、`TextDisplay`エンティティを
3つ、いきものの真上に積み重ねる形にしました。

```
              名前
        めずらしさ　おおきさ
   捕まえた日にち（捕獲した名前）
```

名前の行はそのままの大きさ、下2段（レア度・サイズ／日時・捕獲者名）は少し
小さく（0.65倍）表示しています。日時のフォーマットは既存の図鑑と同じ
「yyyy/MM/dd HH:mm」です。

---

## 68. v0.31.1: トレードの「図鑑未登録の確認」が出ない不具合を修正

### 原因
トレード画面（`TraderListener`）の未登録判定が、**種として図鑑に載っているか**
（`dex.has(creatureId)`）を見ていました。このため、同じ種を以前に別の個体で
登録済みだと、今回売ろうとしているその個体自体は未登録でも、警告が出ない
状態になっていました。

### 修正内容
ご指摘の通り、「新種かどうか」ではなく「**そのアイテム自身に登録スタンプが
付いているか**」（`CatchData#isRegistered()`、`registeredBy`の有無）で判定する
ように修正しました。同じ種を以前に登録していても、その個体自身が未登録なら
きちんと「本当に良いですか？」の確認が出るようになります。

### 図鑑登録側の確認について
ご依頼の通り確認しました。図鑑登録画面（`GuiListener#hasUncaughtBySelf`）は、
アイテムに焼き付けられた捕獲者情報（`CatchData#catcherUuid`）を直接見る作りに
なっており、こちらは今回のような不具合はありませんでした（`catcherUuid`は、
捕獲した瞬間にその場のプレイヤーのUUIDがそのまま焼き付けられることも確認
済みです）。自分以外が捕まえたものを登録しようとすると、正しく確認が出ます。

---

## 69. v0.32.0: マーカーのリセットボタン・生物枠ごとのタグ上書き・サイズ分布テンプレート

ご依頼の3点、まとめて実装しました。

### ①湧いている個体を全てリセットするボタン
マーカー設定画面に「湧いている個体をリセット」ボタンを追加しました。現在の
生存数も表示され、クリックすると、そのマーカーから現在湧いている個体だけを
全てデスポーンさせます（マーカー自体・設定は削除されません）。

### ②生物枠ごとの「必要タグ」上書き
マーカーの各生物枠に、その枠専用の「必要タグ」上書きを設定できるようにしました。
生物枠を**中クリック**すると、チャットでタグをカンマ区切り入力できます
（`none`で上書き解除、`cancel`でキャンセル）。同じ生物でも、マーカーごとに
違う道具でしか捕まえられないようにする、といった使い方ができます
（例：このマーカーのカブトムシだけ`event`タグの網が必要）。

上書きは湧いた個体自身に焼き付けられ、虫取り網・素手・寄ってくる釣り、
3つの捕獲経路すべてで判定されます（上書きが無ければ、creatures.ymlの
`required-tags`がそのまま使われます）。

### ③サイズ分布テンプレート
`config.yml`に`size-distribution-templates`セクションを新設しました。tierごとの
weightだけを上書きした「名前付きの分布」を複数定義でき（例に`big_event`
「大物が増える」・`tiny_only`「ちいさい個体だけ」を同梱）、マーカー設定画面の
「サイズ分布テンプレート」ボタンで、どのテンプレートを使うか選べます
（クリックで巡回。一周すると既定＝`sizes`セクションの重みに戻ります）。
一部のtierだけ書けば、書かなかったtierは既定の重みのまま使われます。

---

## 70. v0.32.1: コンパイルエラー修正・生物枠のスケール/動き上書きを追加

### コンパイルエラーの修正
ご報告のエラーを修正しました。

- `ShowcaseListener.java`：`TextDisplay`のテキスト設定が、このPaperバージョンでは
  `setText(String)`ではなく`text(Component)`（Adventure形式）だったため型エラーに
  なっていました。修正しました。
- `MarkerGuiListener.java`：前回のサイズ分布テンプレート追加の際、`with*`ヘルパーの
  うち`withSpawnInterval`・`withSpawnChance`の2箇所だけ、変数名が他と違う形になって
  いたため、一括置換で見落としていました（引数が15個のままで、期待される16個に
  合っていませんでした）。個別に修正し、今回は全15箇所の引数の数をプログラムで
  数え直して確認済みです。

### 【新規】生物枠ごとのスケール・独特の動きの上書き
ご要望の通り、タグと同じ考え方で、`base-scale`や`behavior`（`flee-from-players`
等）も、マーカーの生物枠ごとに上書きできるようにしました。

生物枠を **Q（ドロップキー）** でクリックすると、チャットで
`key=value` をカンマ区切り入力する形で設定できます。

```
scale=0.8,flee-from-players=true,flee-radius=6,flee-speed=1.2,movement-speed-multiplier=0.5,escape-despawns=true,disable-nectar=false
```

指定できるキー：`scale`・`flee-from-players`・`flee-radius`・`flee-speed`・
`movement-speed-multiplier`・`escape-despawns`・`disable-nectar`。指定しなかった
キーは、creatures.yml本来の設定のままです。`none`で上書き解除、`cancel`で
キャンセルできます。

この上書きは、見た目（scale）と、湧いた瞬間のAI設定（flee-from-players等）
にだけ影響します。価格計算・サイズのcm・タグ判定など、他の仕組みには影響しません
（別の仕組みで扱われています。タグの上書きは、前回実装済みの中クリックの
ままです）。

---

## 71. v0.33.0: 一本釣り（寄ってくる釣り）を大幅に改善

ご要望を全て反映しました。

### ①②③水中判定・隔離判定・パスファインダーによる誘導
直線移動での誘導（壁や陸地を突っ切って近づいてしまっていました）をやめ、
Paperのパスファインダー（`Mob#getPathfinder().moveTo()`）に経路探索を任せる
方式に変更しました。魚のナビゲーションは水中限定のため：

- 浮きが水面に浮いていない、または魚が水中にいない場合は、そもそも対象に
  なりません（キャスト時・待機中・誘導中、常に確認しています）
- 魚と浮きが別々の孤立した水たまりにいる場合、経路が見つからないため
  自然に釣れません（`moveTo`がfalseを返します）
- 迂回すればつながっている場合は、パスファインダーが自然にその迂回ルートを
  通ります

誘導中にAIを無効化する方式もやめました（Pathfinderが無効化状態でも確実に
動く保証が無かったため）。既存のマーカー誘導処理と同じ、AIを保ったまま
定期的に目的地を指示し続ける方式に統一しています。

### ④⑤浮き基準の探索・見つからなくても探し続ける
対象の検索は、以前から浮きの位置を基準にしていました（ご確認いただいた
通りで問題ありませんでした）。ただし、**キャスト直後に何も見つからないと
そこで諦めてしまっていた**不具合があり、これを修正しました。最初に
見つからなくても、`retry-interval-seconds`おきに探し続け、範囲内に
入ってきた個体を見つけ次第判定するようになりました。

### ⑥陸に上がった魚が水へ戻る
誘導中、何らかの理由で魚が陸に上がってしまった場合、近くの水を探し、
ぴちぴちと跳ねるような動き（横方向の速度＋少し上向きの速度）で少しずつ
そちらへ戻すようにしました。水に戻るまでは、浮きへの接近はお預けになります。

### ⑦逃走時に地面に埋まらないように
逃走時の水探し（`findNearbyWaterOrKeep`）を、ランダムサンプリングから
力任せの最短水ブロック探索に変更し、より確実に水を見つけられるようにしました。
水が見つからない場合も、その場が固体ブロックに埋まっていれば、埋まって
いない高さまで自動的に抜け出すようにしています（水中への着地はそのままでOK）。

---

## 72. v0.33.1: 自慢機能が空中クリックで発動しない不具合を修正

### 原因
Bukkit/Paperの公式仕様として、`PlayerInteractEvent`は「バニラの動作が
『何もしない』場合（例：空中とのインタラクション）」は**最初からキャンセル
済みの状態で発火する**、という挙動になっています。いきものアイテムには
特別な使用アクションが定義されていないため、空中でのクリックはまさにこれに
該当していました。`ShowcaseListener`のクリック処理に`ignoreCancelled = true`
が付いていたため、この「キャンセル済み」の空中クリックイベントがハンドラに
そもそも渡らず、自慢が発動していませんでした。

実は、同じ問題は虫取り網（`NetListener`）で以前に対処済みでした
（`ignoreCancelled`を付けない形に既になっていました）。今回、自慢機能にも
同じ対処を適用しました。

### 修正内容
`ShowcaseListener#onInteract`から`ignoreCancelled = true`を外しました。
念のため、`PlayerGiveListener`・`BareHandCatchListener`（`PlayerInteractEntityEvent`
を使用）も確認しましたが、この挙動はあくまで`PlayerInteractEvent`（空中・
ブロックとのやり取り）固有のもので、エンティティを直接クリックする
`PlayerInteractEntityEvent`には当てはまらないため、対処は不要でした。

---

## 73. v0.34.0: 財布の枠数・トレードの自動入金・財布/虫かごの編集中バグ修正

### ①小銭入れの枠を6マスに、中央寄せに
容量を5→6に変更しました。9マス単位のチェスト型GUIが最小単位のため、
「枠そのものを6マスに」は技術的に不可能でしたが、ご提示いただいた代替案の
通り、使える6マスを左詰めではなく**中央寄せ**（左1マス・右2マスを使用不可に）
にしました（`PurseGui#usableSlots`）。

### ②トレードの売上を財布へ自動入金
トレードで確定した売上を、持ち物にある財布・小銭入れへ自動で入れるように
しました。**財布（紙幣・硬貨どちらでも）を優先**して探し、無ければ**小銭入れ
（硬貨のみ）**を探します。財布・小銭入れを持っていない、または入りきらな
かった・条件に合わなかった分（財布が硬貨のみで紙幣が余る等）は、これまで
通り直接持ち物へ渡します（入らなければ足元にドロップ）。

### ③財布・虫かご編集中に、アイテム自体を動かすと中身が溢れる不具合を修正
ご指摘の通りの原因でした。財布・虫かごの画面を開いて中身を編集している
最中に、対象のアイテム自体を持ち物側でクリックして動かしてしまうと、
画面を閉じたときに「元のアイテムへ書き戻す」先を見失い、編集中の中身が
そのまま持ち物へバラバラに溢れてしまっていました。

ご提案いただいた通り、**クリック・ドラッグのたびに、その時点の中身を
実際のアイテムへ同期する**方式に変更しました。これにより、編集中に
アイテム自体を動かされても、直前までの内容は既に正しく書き込まれています。
それでも対象を完全に見失った場合（動かされた先で中身を探せない状態に
なった場合）は、中身を持ち物へ返したうえで画面を自動的に閉じるように
しています。

---

## 74. v0.34.1: 財布・小銭入れの枠を左右対称に統一

ご指摘の通り、左右で塞ぐマス数が違う（左1・右2）のは違和感があったため、
**各行とも左右1マスずつを塞ぎ、中央7マスだけを使う**方式に統一しました
（`PurseGui#usableSlots`を全面的に書き直しています）。

- **小銭入れ**：容量6→**7**（1行9マスのうち、左右端を1マスずつ塞いで中央7マス）
- **財布**：容量9→**14**（2行18マスのうち、各行とも左右端を1マスずつ塞いで、
  7マス×2段＝14マス）

この方式は容量に応じて自動的に行数を計算するため（7マスごとに1行）、
将来また容量を変えたくなった場合も、左右対称のレイアウトのまま自然に
広がります。

---

## 75. v0.34.2: scaleのクランプ位置が間違っていた不具合を修正

### 原因（ご質問いただいた点）
`applyScale`の計算式は `(実測cm ÷ 平均サイズ) × base-scale` ですが、この
**計算結果全体**（base-scaleを掛けた後）を`clamp-min: 0.5`〜`clamp-max: 1.6`の
範囲に丸めてしまっていました。本来このクランプは、config.ymlのコメントに
書かれていた通り「size-min/size-maxの幅が広い生物で、個体差が極端になり
すぎるのを防ぐ」ためのもの、つまり**比率（個体差）の部分だけ**にかけるべき
ものでした。base-scaleを掛けた後の最終結果まで丸めてしまっていたことで：

- **①** base-scaleをどれだけ極端にしても、最終結果が0.5〜1.6の外に出せなかった
- **②** base-scaleが小さい生物（本来小さく見せたい生物）だと、個体差の比率
  （0.8倍〜1.2倍など）にbase-scaleを掛けた結果が両端とも0.5未満になり、
  **両端とも同じ0.5にクランプされて個体差が消えてしまう**ことがあった
  （ダンゴムシのような、size-min/size-maxの幅そのものが狭い生物ほど、
  base-scaleを小さくしたときにこの問題が起きやすくなります）

### 修正内容
ご指摘の通り、「base-scaleの値を基準に、平均サイズからの差（比率）を
掛け算する」という本来の設計に戻しました。

- クランプは、base-scaleを掛ける**前**の「比率」部分にだけかける
  （config.ymlの`clamp-min`/`clamp-max`は、そのままこちらに使われます。
  既定値0.5〜1.6は変えていません）
- base-scaleを掛けた**最終結果**には、明らかに壊れた値（0・負数・極端に
  巨大な値）だけを防ぐ、ごく緩い安全策としてのクランプのみをかける
  （0.0625〜16。Minecraft側のAttribute.SCALEが通常取り得る範囲に合わせた、
  十分に広い範囲です）

これにより、base-scaleの値がそのまま最終的な見た目に反映されるようになり、
ダンゴムシのようなサイズ幅の狭い・小さい生物でも、個体差がきちんと見た目に
出るようになります。

---

## 76. v0.34.3: 財布・虫かご編集中に対象を動かすとアイテムが分裂する不具合を修正

### 原因
前回（v0.34.0）、編集中に対象のアイテム自体を動かされても中身を見失わない
よう、「クリック・ドラッグのたびに、その時点の中身を実際のアイテムへ同期する」
仕組みを入れました。ところが、対象を見失った（動かされて元の場所に無くなった）
ときの後始末が「中身を持ち物へ返す」処理になっていたため、**既に同期済みの
中身**（動かされた先の財布・虫かごの中に、既に正しく保存されている）に加えて、
**もう一度同じものを持ち物へ渡してしまい**、アイテムが分裂していました。

### 修正内容
ご指摘の通り、「中身が既に保持されているなら、持ち物には戻さない」ように
修正しました。対象を見失った場合は、中身を持ち物へ渡す処理を行わず、画面を
閉じるだけにしています（画面を閉じたときの後始末も同じ理由で修正しています）。

---

## 77. v0.35.0: 自慢機能を複数体対応に（上限5・連続召喚・名前設定・空中配置調整）

ご要望を全て反映しました。

### ①上限5匹・④連続召喚
これまで1人1体までだった仕組みを、1人**最大5体まで**同時に表示できるように
作り直しました。構え成立後、**スニークを押し続けている限り**構えの判定を
リセットしなくなったため、そのまま連続で右クリックすれば次々と表示できます
（5体に達すると、それ以上は表示できない旨のメッセージが出ます）。距離判定
（動くと消える）も、表示した個体それぞれについて独立に行うようにしています。

### ③名前を設定
ご指摘の通り、表示するエンティティに名前（カスタムネーム）を設定していな
かったため、名前を基準に見た目を切り替えるタイプのリソースパック（CEM等）が
反応していませんでした。実際のいきもの湧き（`SpawnService`）と同じように、
生物名を設定しています（表示自体は引き続き非表示のままです）。

### ②空中での配置
見上げているときに、視線の角度（pitch）をそのまま反映すると高く出過ぎて
しまう問題を修正しました。角度に比例させるのではなく、「見上げていれば
固定で少しだけ（既定1ブロック）底上げする」だけに留めています。

### ホールド時間
1.5秒→**1.0秒**に短縮しました。

---

## 78. v0.35.1: 自慢表示の色を調整

ご指定の通り、2段目の「おおきさ」の色を白（WHITE）に、3段目の日付の色を
グレー（GRAY。以前の「おおきさ」と同じ色）に変更しました。

---

## 79. v0.36.0: マーカーの生物枠選択を一覧画面方式に刷新

ご要望の通り、マーカー設定画面の生物枠の選び方を大きく改善しました。

### ①一覧から選ぶ方式に
左クリックで「名前が順々に切り替わる」方式をやめ、左クリックで**生物の一覧
画面**（`CreaturePickerGui`）を開き、そこから直接選べるようにしました。
図鑑と同じ考え方でページ送りに対応しているため（1ページ45件）、生物の種類が
増えても迷わず選べます。既存の枠を選び直した場合、ウェイト・タグ上書き・
スケール上書きはそのまま引き継がれ、生物の種類だけが差し替わります
（右クリック＝ウェイト-1、中クリック＝タグ編集、Qクリック＝スケール/動き編集は、
これまで通り変更ありません）。

### ②アイコンの素材変更
生物枠のアイコンを、紙（PAPER）から**ウサギの皮（RABBIT_HIDE）**に変更し、
`custom-model-data`にも対応しました。`config.yml`の`ambient-spawn`セクションの
`creature-slot-material`（素材）・`creature-slot-custom-model-data`（任意）で
変更できます。

---

## 80. v0.37.0: マーカーの生物枠を「一覧選択」と同じ考え方で詳細設定も刷新

ご要望通り、生物枠の操作を大きく作り直しました。

### 「枠を空にする」を一覧画面へ移動
シフト＋左クリックだった「この枠を空にする」を、前回作成した生物一覧画面
（`CreaturePickerGui`）内の専用ボタンに移しました。

### シフト＋左クリック＝詳細設定画面
空いたシフト＋左クリックは、**詳細設定画面**（`CreatureDetailGui`）を開く
操作にしました。creatures.ymlで設定できる項目のうち、マーカーの生物枠ごとに
上書きできるものを、1項目1マスのボタンとして並べています。

- **true/falseの項目**（`flying`・`flee-from-players`・`escape-despawns`・
  `disable-nectar`）：クリックでそのまま切り替え（上書き無し→true→false→
  上書き無し、の3段階で巡回）
- **数値・タグの項目**（必要タグ・`scale`・`escape-chance`・`flee-radius`・
  `flee-speed`・`movement-speed-multiplier`）：クリックしてからチャットに
  入力する形（`none`で解除、`cancel`でキャンセル）
- 「この枠の上書きを全て解除」ボタンも用意しています

以前の中クリック（タグ編集）・Q（ドロップ、スケール/動き編集）は廃止し、
この詳細設定画面に一本化しました。

### 新しく上書きできるようになった項目
ご要望の`flying`・`escape-chance`も、マーカーの生物枠ごとに上書きできるように
なりました。湧いた個体のPDCに焼き付けて、実際の捕獲判定（虫取り網・素手・
寄ってくる釣り）のときはそちらを見るようにしています（`required-tags`と
同じ考え方です）。

---

## 81. v0.37.1: コンパイルエラー修正

`MarkerGuiListener.java`に`Creature`のimportが不足していました（一覧画面での
選択結果を表示する箇所で使用）。追加しました。念のため、今回の一連の変更で
触った全ファイルについて、使っている型とimport文の対応を一通り確認済みです。

---

## 82. v0.38.0: バグ修正5点（一旦こちらのビルドを先にお渡しします）

ご報告いただいたバグのうち5点を修正しました。大規模な追加要望（詳細設定画面の
レイアウト刷新・サイズイベントの枠ごと化・追加の上書き項目）は、規模が大きいため
続けて対応中です。

### ①「逃げると完全に消える」の上書きが効かない不具合
原因は、`required-tags`等と同じパターンでした。マーカー枠の上書きは、湧いた
個体自身に焼き付けて捕獲・逃走の瞬間に参照する仕組みが必要でしたが、
`escape-despawns`だけはこの焼き付けが漏れており、常にcreatures.yml本来の
設定を見てしまっていました。他の上書き項目と同じ方式で焼き付けるようにしました。

### ②「必要タグ」の上書き表示が、他の項目を変えると変わって見える不具合
詳細設定画面の表示ロジックに誤りがありました。必要タグの表示が、タグ自体では
なく「他の上書き項目（scale等）が何か設定されているか」も条件に含んでしまって
いたため、タグを変えていなくても表示が「設定済み」に見えてしまっていました
（実際のデータが変わっていたわけではありません）。表示ロジックを修正しました。

### ③表示を羽ペン（本）ではなく、素材そのまま＋エンチャント風の光沢に変更
ご指定の通り、上書きが設定されている項目は、素材を書き換えるのではなく、
元の素材のまま光沢（エンチャント風の見た目）だけを付けるようにしました。
true/falseのボタン（色分け）はそのままです。

### ④プレイヤーから離れてデスポーンする際、追われている個体は残す
マーカーの中心からプレイヤーが離れた場合の一括デスポーンを、個体ごとの判定に
変更しました。マーカーからは離れていても、その個体自身の近くにプレイヤーが
いる場合（追いかけている最中とみなせる場合）は、その個体だけデスポーンしない
ようにしています。

### ⑤「近づくと逃げる」の速度が、蜂・魚に反映されない不具合
調査の結果、Minecraft本体の仕様として、飛ぶ生物・泳ぐ生物は移動速度の指定
（Attribute.MOVEMENT_SPEEDはもちろん、飛行専用のAttribute.FLYING_SPEEDですら）が
正しく反映されないという、公式のバグ報告（例: MC-172801「Bee's do not use the
flying speed attribute」）で確認できる既知の制限があることが分かりました。
これらの種別（Bee・Fish系）に限り、パスファインダーの速度指定には頼らず、
毎tick直接velocity（速度ベクトル）を与える方式に切り替えることで、確実に
speedの設定が反映されるようにしました。

---

## ビルドについて

このzipはMaven等でのビルドは行っていません（このチャット環境にネットワーク接続が
なく、Paper APIの依存関係を取得できないため）。お手元の開発環境で
`mvn clean package` を実行し、実機（テストサーバー）で動作確認をお願いします。
コンパイルエラーや動作不具合があれば、エラーメッセージを共有いただければ
そのまま修正します。

### 既知の修正履歴
- v0.2.0公開直後、`Sound.ITEM_BREAK`という存在しない定数を使っておりコンパイルエラーに
  なっていました。正しい定数名`Sound.ENTITY_ITEM_BREAK`に修正済みです。
- 今回（v0.2.1）新たに`Sound.ENTITY_FISHING_BOBBER_SPLASH`を使用しています。もしこれも
  存在しない場合は、エラーメッセージを送ってください。同様の修正を行います。
